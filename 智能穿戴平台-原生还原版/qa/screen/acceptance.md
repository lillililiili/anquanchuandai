# 数据大屏纯展示模式验证

最新范围：按用户要求移除大屏内所有操作；保留 3 秒自动轮播。

- 浏览器验证 28 项通过；脚本错误 0，失败资源 0。原始结果见 [browser-checks.json](browser-checks.json)。
- DOM 中没有按钮、链接、下拉框、可编辑字段、tabindex 或业务操作标记；头部悬停也不出现工具栏。
- 点击品牌、人员、地图标记、事件、视频，以及 Tab/Enter/空格均不产生页面业务操作；没有预览弹窗。
- 三组均在 3 秒自动切换，8 人 24 秒一轮；旧版保存的暂停记录不会锁住轮播。
- 身份、指标、时间及心率趋势同步；自动刷新、离线空值、异常插播及持续提示正常。
- 1366×768、1672×941、1920×1080 适配通过；空厂站隔离、刷新和浏览器后退清理定时器正常。
- 新版检查脚本已取代原交互式检查，不再验证已移除的暂停、选人、全屏和预览操作。

实际截图：[当前大屏](dashboard.png)、[1366 视口](dashboard-1366.png)、[空厂站](empty-station.png)。

原 comparison 与 final-comparison 目录是旧交互版的历史对照，不能作为当前控件状态证据。当前视觉检查以 dashboard.png 为准。本次保留原布局，只移除操作内容并将姓名等改成静态文字。素材和字体原有差异仍在，不宣称逐像素完全一致。现场画面仍为明确标注的演示图片。
