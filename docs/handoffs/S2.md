# S2 交接单（给 Android 协作者 B）

日期：2026-09-08  
契约：[s2-people.md](../contracts/s2-people.md)  
依赖：S1 已完成。本机测试库已执行 `V001` + `V002`。

## 联调

在 S1 环境上再执行：

`后端代码/melhat_server-dev/sql/migrations/V002__person_org_space.sql`

- 后端 `http://127.0.0.1:18084`
- 厂站头 `X-Site-Id` 同 S1，服务端校验
- 人员：`GET /api/v1/people`、`GET /api/v1/people/{id}`、`GET /api/v1/people/options`
- 区域：`GET /api/v1/spaces`
- 班组/承包商：`GET /api/v1/teams`、`GET /api/v1/contractors`

**不要用** `/system/user/list` 选人。

## 样例

| 人员标识 | 说明 |
| --- | --- |
| `P-DEMO-001`～`003` | A 站，有登录账号（demo01～03） |
| `P-DEMO-004`～`006` | B 站 |
| `P-DEMO-NOACCT` | 无账号；姓名与 001 同为「演示人员01」，用 `id`/`personCode` 区分 |
| `P-DEMO-EXPIRED` | 有效期 2020-01-01，`selectable=false`，选择器不出现，详情仍可打开 |

账号（密码与 `admin` 相同）：`wear_admin`、`siteA_duty`、`siteB_duty`、`siteA_readonly`。

- A 站值班员看不到 B 站人员；读 B 详情 HTTP 403
- 只读不能 POST `/api/v1/people`（HTTP 403）；隐藏按钮 ≠ 已授权
- 停用不删历史，详情仍按稳定 ID 打开

## B 本轮应接

本组人员列表、人员详情、人员选择器（仅 `selectable=true`）、区域名称查询。装备区未交付（S4）。

## 未稳定

通用设备台账、领还、统一事件、SOS 接听、安全带。帽子当前绑定人仍是旧 `sys_user`，S4 再切到 Person。
