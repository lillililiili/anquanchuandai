# S3 交接单（给 Android 协作者 B）

日期：2026-09-08  
契约：[s3-devices.md](../contracts/s3-devices.md)  
依赖：S1、S2。本机测试库已执行 `V001` + `V002` + `V003`。

## 联调

在已有环境再执行：

`后端代码/melhat_server-dev/sql/migrations/V003__device_and_model.sql`

- 后端 `http://127.0.0.1:18084`
- 厂站头 `X-Site-Id` 同 S1
- 设备：`GET /api/v1/devices`、`GET /api/v1/devices/{id}`
- 型号：`GET /api/v1/product-models`

入口以返回的 `capabilities.actions` / `attributes` / `events` 为准。**禁止**写死「安全帽一定有视频」。

## 样例

| SN | 说明 |
| --- | --- |
| `MH-DEMO-001`～`003` | A 站摄像帽，映射旧帽，`legacyHatId` 有值，连接未知 |
| `MH-DEMO-004`～`006` | B 站摄像帽 |
| `MH-DEMO-NOV-A` | A 站无视频帽，`actions` 无 `video` |
| `BL-DEMO-001` / `002` | A/B 站安全带 |
| `MH-UNASSIGNED` | 未分配，仅平台管理员可见 |

账号（密码与 `admin` 相同）：`wear_admin`、`siteA_duty`、`siteB_duty`、`siteA_readonly`。

- A 站值班员看不到 B 站设备与未分配设备（详情 403）
- 只读不能 POST `/api/v1/devices`
- 同一厂商重复 SN → 409
- 新建设备 `online`/`battery`/`lastReportedAt` 为 `null`（未知，不是离线）

旧 `/hat/safety/info/*` 仍可用，帽号未改。正式切流在 S11。

## B 本轮应接

设备列表、详情、按型号能力控制视频/对讲等入口。领用等 S4。

## 未稳定

领还、统一事件、真实帽/带接入、SOS 接听。隐藏按钮 ≠ 已授权。
