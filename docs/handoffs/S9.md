# S9 交接单（给 Android 协作者 B）

日期：2026-09-09  
契约：[s9-work.md](../contracts/s9-work.md)  
依赖：S1–S7。测试库再执行 `V008__work_task_and_duty.sql`。

最小作业任务与值班待办。**不是**工作票。无票显示待核实，不是违章。任务结束 **不**关闭事件。`demo: true` 不得当生产作业。

旧 `/todo/tasks/**` 仍给旧页，不要当本模块真相。

## 联调

- `GET /api/v1/work-tasks`、`GET /api/v1/work-tasks/mine`、`GET /api/v1/work-tasks/{id}`
- 详情含 `members`、`requirements`、`equipmentCheck`（`missing`/`ok`/`unknown`）
- `GET /api/v1/work-tasks/{id}/events`
- 写操作仅值班/班组长：创建、成员、`start`/`pause`/`end`（高风险未关闭须 `acknowledgeOpenHighRisk`）
- 事件：`taskId`、`taskMatch`=`none|matched|pending`；`POST /api/v1/events/{id}/task`
- `GET /api/v1/duty/summary`：仅当前厂站。`peopleCount` 与 `deviceCount` 分开
- 交接：`POST /api/v1/duty/handovers` 为 pending；`POST .../confirm` 由接班人确认后才改责任

隐藏按钮 ≠ 已授权。跨站 403。

## 样例（密码 `admin123`）

- `siteA_duty`：可建任务、交接
- `siteA_team_lead`：可确认交接
- `siteA_readonly`、`wear_admin`：写 403
- 种子：「东区巡检」（演示，本机验收中已结束）、「高处待核实票」草稿

## B 本轮应接

我的任务、任务详情、装备检查、值班 summary。复杂建任务留 PC。安全带检查为未知时不要画成已扣好。

## 未稳定

安全带协议、作业 WebSocket。围栏实时见 S10（帽 GNSS，精度未确认）。
