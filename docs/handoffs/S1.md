# S1 交接单（给 Android 协作者 B）

日期：2026-09-08  
契约：[s1-auth.md](../contracts/s1-auth.md)

## 联调

- 后端 `http://127.0.0.1:18084`
- 测试库需已执行 `后端代码/melhat_server-dev/sql/migrations/V001__site_and_account_grant.sql`
- 登录仍用 `POST /login`；当前用户与厂站用 `/api/v1/me`、`/api/v1/sites`、`PUT /api/v1/me/current-site`
- 业务请求可带 `X-Site-Id`（必须在授权列表中）

## 测试账号（密码与 admin 相同，向维护者确认；种子使用若依默认 hash）

| 账号 | 角色 | 厂站 |
| --- | --- | --- |
| `wear_admin` | 平台管理员 | A+B |
| `siteA_duty` | 值班员 | A |
| `siteB_duty` | 值班员 | B |
| `siteA_readonly` | 只读 | A |
| `admin` | 若依超管 | A+B（种子授权） |

演示帽子：`MH-DEMO-001..003` 属 A，`004..006` 属 B。

## B 本轮应接

登录、选厂站、会话清理、401/403。帽子列表可按当前厂站过滤验证，但 Android 不必复制 PC 资产维护页。

## 未稳定

人员、通用设备、领还、统一事件、SOS 接听、安全带。隐藏按钮 ≠ 已授权。
