# S0 验收记录

对照 [后端与PC管理端分阶段详细开发计划](../planning/后端与PC管理端分阶段详细开发计划.md) 第 3 节。

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S0-01 基线与历史台账 | 文档已写 | `sql/wearables` 未找回 |
| S0-02 工具链与构建命令 | 文档已写 | JDK 8 目标 / 本机 JDK 17；Boot 插件 2.1.1 与 2.5.14 不一致已记录 |
| S0-03 配置边界 | 代码+文档 | 仓库 yml 无生产明文库密码；demo-mode 默认 false；演示数据 `MH-DEMO-*` |
| S0-04 迁移目录 | 已建约定 | 无业务 DDL |
| S0-05 password 不出现在 HTTP | 单测 + 运行验收 | `SysUser` 序列化忽略；`getInfo` 用安全 DTO |
| S0-05 演示隔离 | 注解 + 单测 | `@Profile("!prod")` 且 `melhat.demo-mode=true` |
| S0-05 匿名入口 | 配置 + 单测 + 抽测 | 见交接单；`/ws/**` 在 dev 仍匿名 |
| S0-05 下载代理下限 | 代码 + 单测 | 协议/私网/超时/大小/重定向 |
| S0-05 日志敏感信息 | 抽查 | 登录失败不打印明文密码；回调 filter 不打印 token |
| S0-06 后端测试与健康检查 | 单测 + actuator | `GET /actuator/health` |
| S0-06 PC 测试 setup | 已补 | `npm test` 不再因缺 setup 失败 |
| S0-07 契约草案 | 已写 | 旧接口并存 |
| S0-08 硬件清单 | 已写 | 无假装已对接 |
| PC 标题 | 已改 | 「智能穿戴管理平台」 |
| SOS 假接通 | 已去 | 按钮禁用并提示 S7 |

## 本机实测（2026-09-08）

- `mvn -pl ruoyi-admin -am test`：26 tests, 0 failures（含原有演示测试；admin 已用 Surefire 2.22.2，否则 JUnit 5 会计为 0）
- `npm test`：2 tests passed
- `GET /actuator/health` 匿名：`{"status":"UP"}`
- `GET /getInfo` 匿名：业务码 401
- `POST /hat/safety/info/save` 匿名：业务码 401
- `POST /ext/helmetAlarm` 无回调头：HTTP 401
- `GET /monitor/job/list` 匿名：业务码 401
- 登录 `admin` 后 `GET /getInfo`：`user.userName=admin`，响应体无 `password` 字段
- PC `http://127.0.0.1:5175` 标题与侧栏为「智能穿戴管理平台」；SOS 页无「呼叫已接通」；有限空间页显示未交付提示

未在本表勾“已跑”的检查，以交接单为准，不得把文档勾选当成生产已上线。
