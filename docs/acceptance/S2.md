# S2 验收记录

日期：2026-09-08（本机）  
库：`melhat_local`，已执行到 `V002__person_org_space.sql`

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S2-01 Person 独立表 | 通过 | `wear_person`，`person_code` 唯一，ID 字符串 |
| S2-02 无账号人员 | 通过 HTTP + PC | `P-DEMO-NOACCT`；POST 可不填 `accountUserId`；PC 新增 `P-UI-NOACCT` |
| S2-03 人员厂站授权与可选判定 | 通过 | `wear_person_site`；调离保行；`PersonEligibility` 单测 3 条 |
| S2-04 班组/承包商 | 通过 | `wear_team` 隶属厂站；`wear_contractor` 不是厂站 |
| S2-05 Space 资料 | 通过 | `wear_space` 区域/设施/楼层，页面写明非实时定位 |
| S2-06 分页/详情/选择器 | 通过 | `/api/v1/people*`；选择器不走 `/system/user/list` |
| S2-07 批量导入 | 不做 | 首期逐条维护 |

隐藏按钮不作为授权证明。只读账号页面无新增/编辑/停用，HTTP POST 仍 403。

## 单测

- Maven `ruoyi-admin`：Tests run **35**，Failures **0**（含 `PersonEligibility` 3、`WearRoleKeys` 人员写入）
- PC Vitest：**4** passed（含 `people.test.js` 断言选择器不使用系统用户列表）

## 本机 HTTP（`127.0.0.1:18084`）

- `siteA_duty` `/api/v1/me`：有 `wear:person:list`，无 `wear:person:edit`
- `siteA_duty` 人员分页：含 `P-DEMO-001` / `P-DEMO-NOACCT` / `P-DEMO-EXPIRED`，不含 `P-DEMO-004`；同名「演示人员01」两条不同 `id`
- `P-DEMO-EXPIRED.selectable=false`；`/people/options` 不含过期与 B 站人员，含无账号人员
- `siteA_duty` 读 B 站人员详情 `GET /api/v1/people/4`：HTTP **403**
- `siteA_readonly` `POST /api/v1/people`：HTTP **403**
- `siteB_duty` 人员分页不含 A 站人员；班组不含「A站巡检班」；区域不含「东区作业区」
- `wear_admin` 创建无账号人员后停用：详情仍在，`selectable=false`；过期 `version` 更新 HTTP **409**
- `siteA_duty` `POST /api/v1/teams`：HTTP **403**

## PC 浏览器（`http://127.0.0.1:5175`）

- `wear_admin` 菜单「业务管理 / 人员」「区域资料」可进
- 未选厂站时人员并集可见；选「演示厂站A」后列表 6→不含 `P-DEMO-004..006`
- 无账号人员详情：账号「无」，装备区标明 S4 接入
- 过期人员行提示「有效期已过」
- 页面新增 `P-UI-NOACCT`（不填登录账号）成功；人员接口未请求 `/system/user/list`
- `siteA_readonly` 人员页仅「详情」，无新增/编辑/停用；区域页无「新增区域」
- 帽子页仍按 S1 厂站过滤（A 站 3 顶）；绑定人仍走旧 `/system/user/list`（S4 再换 Person）

## 未做

S3 设备台账、S4 领用、批量导入。帽子绑定人仍是 `sys_user`。
