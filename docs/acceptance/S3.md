# S3 验收记录

日期：2026-09-08（本机）  
库：`melhat_local`，已执行到 `V003__device_and_model.sql`

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S3-01 类型与型号能力 | 通过 | `helmet`/`belt`；能力在型号 JSON，无类型名自动视频 |
| S3-02 Device 台账 | 通过 | `wear_device`，ID 字符串，帽号作 `external_code` |
| S3-03 SN 唯一 | 通过 HTTP 409 | 约束 `(manufacturer_code, sn)`；重复 SN 不落第二条 |
| S3-04 登记/分页/详情/分配厂站 | 通过 | 未分配仅平台管理员 |
| S3-05 资产 ≠ 连接 | 通过 | 新建/种子 `online`/`battery`/`lastReportedAt` 为 null，UI「未知」 |
| S3-06 型号能力 | 通过 | `HAT-MH-NOV` 无 `video`；PC 无视频入口 |
| S3-07 旧帽映射预览 | 通过 | `legacyHatId` 对上 MH-DEMO-001..006；帽号未改；预览只读 |

隐藏按钮不作为授权证明。只读 POST 设备 HTTP 403。

## 单测

- Maven `ruoyi-admin`：Tests run **39**，Failures **0**（含 `DeviceCapability` 3）
- PC Vitest：**6** passed（含 `devices.test.js`：类型 helmet 不等于有视频）

## 本机 HTTP（`127.0.0.1:18084`）

- `siteA_duty` `/me`：有 `wear:device:list`，无 `wear:device:edit`
- A 站列表含帽 `MH-DEMO-001`、无视频帽 `MH-DEMO-NOV-A`、带 `BL-DEMO-001`；不含 B 站与 `MH-UNASSIGNED`；连接字段为 null
- A 读 B 设备、读未分配设备：HTTP **403**
- `HAT-MH-NOV` 详情 `actions` 无 `video`；摄像型号有 `video`
- `siteA_readonly` POST 设备：HTTP **403**
- 重复 SN：HTTP **409**
- 新建设备连接为 null；过期 version：HTTP **409**
- 旧帽预览：值班 403，平台管理员 200
- `siteB_duty` 含 `BL-DEMO-002`，不含 A 站设备

## PC 浏览器（`http://127.0.0.1:5175`）

- `wear_admin` 菜单「设备台账」可进；帽带同表；连接列「未知」
- 未选厂站可见未分配 `MH-UNASSIGNED`；选「演示厂站A」后 7 条，不含 B 站与未分配
- `MH-DEMO-NOV-A` 详情：「本型号无视频能力，不提供视频入口」；领用/遥测/通话仅为 S4/S6/S7 说明
- 旧「安全帽运行状态」菜单仍在

## 未做

S4 领还、真实在线、停用旧帽页、改旧表帽号。
