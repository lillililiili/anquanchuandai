# S6 验收记录

日期：2026-09-09（本机）  
库：`melhat_local`，已执行到 `V006__helmet_ingest.sql`

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S6-01 HelmetAdapter | 通过 | 封装 `/ext` 三入口，帽号映射 `wear_device` |
| S6-02 回调认证 | 通过 | 无 token 401；HMAC 单测假签/过期时间戳失败 |
| S6-03 先落库再应答 | 通过 | `wear_ingest_raw` 有 accepted/duplicate |
| S6-04 乱序 | 通过 HTTP | 更早 GNSS 不把 `lastReportedAt` 打回旧时间 |
| S6-05 新鲜度 | 通过 | 无上报=未知；有上报=有效；不显示离线 |
| S6-06 定位 | 通过 | GNSS 样本带坐标；SOS/坠落缺坐标不阻断 |
| S6-07 重放 | 通过 | `POST /api/v1/ingest/replay` 坠落事件 `source=helmet` `demo` |

SOS 回调已去掉自动 `agoraToken`。PC 无「呼叫已接通」。

## 单测

- Maven：Tests run **54**，Failures **0**（含 `HelmetCallbackAuth` `HelmetTypeMapping` `TelemetryFreshness`）
- PC Vitest：devices + events **5** passed

## 本机 HTTP

- `/ext/sosCall` 无 token → 401  
- 带 `X-Melhat-Callback-Token: local-s6-callback` 对 `MH-DEMO-001`：统一 SOS 事件，人员 `P-DEMO-001`，重复报文 `repeatCount>=1`  
- 先新 GNSS 再旧 GNSS：`lastReportedAt` 不含旧时刻  
- 设备 `source=live`，`connectionQuality` 为 ok/stale  
- 重放 fall 后事件列表可见 `source=helmet`

## PC

- 事件工作区坠落详情：来源「设备上报」，快照帽 `MH-DEMO-001`  
- 设备台账：`MH-DEMO-001` 连接「有效」；未上报帽「未知」不是离线  
- 详情：最近样本、接入摘要、数据来源 live；通话仍标 S7

## 未做

RTC/TTS 回执、安全带、围栏实时判定、HMAC 作为现场强制（待供应商书面确认）、样机真实现场联调记录。
