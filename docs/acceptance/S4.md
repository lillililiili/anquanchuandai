# S4 验收记录

日期：2026-09-08（本机）  
库：`melhat_local`，已执行到 `V004__assignment_and_lifecycle.sql`

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S4-01 Assignment | 通过 | `wear_assignment` 历史追加，不覆盖 |
| S4-02 一设备一条有效领用 | 通过 HTTP 409 | 第二人领同一设备失败 |
| S4-03 帽带并用、同类限制 | 通过 | `P-DEMO-001` 同时帽+带；再领第二顶帽 409 |
| S4-04 领用检查 | 通过 | 过期人员 409；只读 403 |
| S4-05 维修不覆盖领用 | 通过 | 已领用改维修/调拨 409 |
| S4-06 幂等与审计 | 通过 | 相同 `idempotencyKey` 返回同一条 |
| S4-07 当前装备与历史 | 通过 HTTP + PC | 人员详情同时显示帽和带 |
| S4-08 有领用禁调拨 | 通过 | 409「请先归还或走异常回收」 |

## 单测

- Maven `ruoyi-admin`：Tests run **40**，Failures **0**（含 `SameTypeSlot`）
- PC Vitest：**6** passed

## 本机 HTTP

- 种子：`MH-DEMO-001` 与 `BL-DEMO-001` 已领用；`MH-DEMO-002` 在库
- `GET /people/1/equipment` 同时含帽和带
- 再领第二顶帽给 `P-DEMO-001`：409
- 只读 POST 领用：403
- 过期人员领用：409
- 相同幂等键两次 POST `MH-DEMO-002`：同一 `id`
- 第三人再领该设备：409
- 已领用设备调拨/改维修：409
- 归还后历史 `returnedAt` 非空

## PC

- 设备台账：已领用显示「归还」，在库显示「领用」
- 人员详情：当前装备安全帽 `MH-DEMO-001` + 安全带 `BL-DEMO-001`

## 未做

S5 事件、真实遥测、旧帽 `bind_user_id` 切换。
