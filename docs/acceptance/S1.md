# S1 验收记录

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S1-01 当前用户 DTO | 代码 | `/api/v1/me` 无 password |
| S1-02 厂站与授权表 | 迁移 V001 | `wear_site` / `wear_site_account` |
| S1-03 角色矩阵 | 契约 | 见 s1-auth.md |
| S1-04 me/sites/选站 | 代码 | 厂站参数服务端校验 |
| S1-05 公共校验 | 代码 | 帽子 page/list/detail/save 已接 |
| S1-06 停用/退出失效 | 代码 | Redis 会话删除 + disabled 标记；PC WS 主动关闭 |
| S1-07 两站种子与反例 | 已跑 HTTP | 见下方 |

隐藏按钮不作为授权证明。

## 本机 HTTP（melhat_local，V001 已执行）

- `siteA_duty` `/api/v1/me`：无 password；仅 `SITE-DEMO-A`；无 `wear:hat:edit`
- `siteA_duty` 帽子分页：仅 MH-DEMO-001..003，不含 004
- `siteA_duty` 读取 B 站帽子详情：HTTP 403
- `siteA_readonly` 保存帽子：HTTP 403「没有权限执行该操作」
- 退出后同一 token 调 `/api/v1/me`：业务码 401
- 停用 `siteA_duty` 后旧 token 调 `/api/v1/me`：业务码 401（随后已恢复账号）
- `wear_admin` 带 `X-Site-Id` 可分别看到 A/B 帽子
