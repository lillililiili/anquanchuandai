# S10 验收记录

日期：2026-09-09（本机）  
库：`melhat_local`，已执行到 `V009__location_and_fence.sql`

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S10-01 一人一主位置 | 通过 HTTP+PC | 当前领用帽最近 GNSS；无帽/无点仍返回人，坐标空，`locationQuality=unknown`；`floor` 恒空，`floorSource=unknown` |
| S10-02 轨迹 | 通过 HTTP | `from`/`to`，当时领用帽样本，上限 500；跨站 403 |
| S10-03 多边形围栏 | 通过 | `wear_geo_fence*`；种子「东区演示围栏」覆盖 36.12,117.12，`debounceSeconds=0` `demo=true` |
| S10-04 质量不足不违章 | 通过单测+HTTP | `unknown`/`stale` 及超过陈旧窗的点不发 geofence 事件 |
| S10-05 规则版本与禁用 | 通过 HTTP | 改几何 `ruleVersion` 升；旧事件 `ruleVersion` 不变；禁用后该围栏不再新增事件，历史保留 |

安全带不打点。H-SITE-02 精度需求未书面确认，不得标已达标。`demo: true` 不得当生产在线。旧 `/hat/electronic/fence` 未切流。

## 单测

- Maven `ruoyi-admin -am`：Tests run **68**，Failures **0**（含 `PointInPolygon` `GeofenceDebounce` `PersonLocationSource`）
- PC Vitest：`locations.test.js` **2** passed（全套 15 passed）

## 本机 HTTP

- `GET /api/v1/locations/people`：P-DEMO-001 来源 `helmet`，楼层未知
- 跨站轨迹 `X-Site-Id=2` → **403**
- `siteA_duty` / `siteA_readonly` `POST /api/v1/fences` → **403**
- `wear_admin` 可创建围栏
- 有效 GNSS 写入 `type=geofence` `source=geofence`，带 `fenceId`/`fenceAction`/`ruleVersion`
- 陈旧 GNSS 不新增围栏事件
- 禁用种子围栏后再上报，该围栏无新事件；旧事件仍在

## PC

- `siteA_duty`：人员位置质量标签；详情来源安全帽、楼层未知；围栏规则无新增/禁用
- 事件详情：`围栏：#… enter/leave　规则 fence-…`
- `siteA_readonly`：围栏规则无保存按钮
- `wear_admin`：新增围栏、禁用
- 旧电子围栏搜索后 URL 仍含 `#/business/fence`

## 未做

安全带位置（S8）、楼层图、圆形围栏、H-SITE-02 精度达标声明、把旧电子围栏切到新表。
