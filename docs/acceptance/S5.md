# S5 验收记录

日期：2026-09-09（本机）  
库：`melhat_local`，已执行到 `V005__safety_event.sql`

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S5-01 SafetyEvent/Action | 通过 | 新表 `wear_safety_event` / `wear_event_action`，快照写死 |
| S5-02 旧告警映射 | 通过结构 | `wear_event_legacy` 已建；本轮不切流，旧表仍给旧页 |
| S5-03 状态机 | 通过单测+HTTP | 高风险 handle→pending_review；低风险 handle→handling→close |
| S5-04 原子认领 | 通过 HTTP | `siteA_duty` 与 `siteA_team_lead` 同 version：一 200 一 409 |
| S5-05 列表/详情/inbox | 通过 | 默认未关闭；跨站 403；`inbox/count` 有 count |
| S5-06 WS 只通知 | 通过日志 | 按 userId `_1`/`_2` 定向 `wear.event`，不 `sendAllMessage` |
| S5-07 SOS 升级 | 通过 | open SOS 超 5 分钟插入 `escalate`，仍可认领 |
| S5-08 模拟器 | 通过 | `demo: true`；同一 `sourceEventId` 只一条且 `repeatCount>=1` |

## 单测

- Maven `ruoyi-admin`：Tests run **47**，Failures **0**（含 `EventStateMachine` 5、`WearRoleKeys` 7）
- PC Vitest：`events.test.js` + 既有 identity **3** passed（本轮新增 2）

## 本机 HTTP

- A 值班列表可见 `seed-sos-a`，默认不含已关闭
- `X-Site-Id=2` 对 A 值班 403；B 值班列表无 A 站 SOS
- 双账号认领同一 SOS：200 + 409，刷新 claimant 相同
- 值班关高风险 403；认领人 handle → `pending_review`；`siteA_reviewer` close 200
- 围栏：认领→处置 `handling`→关闭 `false_alarm` 200
- 模拟器两次同一 id：同一事件，`repeatCount>=1`，`demo: true`
- `siteA_readonly`、`wear_admin` 认领 403
- `/api/v1/me`：值班有 `wear:event:claim`，平台有 `wear:event:review` 无 claim

## PC

- `siteA_duty` 菜单「安全事件」：左列表右详情
- 认领演示 SOS 后列表为「已认领」，时间线「已认领 siteA_duty（待认领 → 已认领）」
- 快照为 `P-DEMO-001` / `MH-DEMO-001`，`demo` 标记，位置未知
- 人员页仍可用；旧 SOS 菜单保留
- 只读写操作 HTTP 403；页面按 `wear:event:claim` 隐藏认领按钮

## 未做

真实帽接入（S6）、RTC（S7）、旧告警切流、改造 `/ws/**` 匿名策略。
