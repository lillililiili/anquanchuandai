# S7 验收记录

日期：2026-09-09（本机）  
库：`melhat_local`，已执行到 `V007__call_and_command.sql`

| 项 | 结果 | 说明 |
| --- | --- | --- |
| S7-01 CallSession / DeviceCommand | 通过 | 新表 `wear_call_session` / `wear_device_command`；旧 `/hat/intercom` `/hat/tts` 未切流 |
| S7-02 认领 ≠ 接通 | 通过 HTTP+PC | 认领后 `GET /events/{id}/calls` 为空；通话结束后 SOS 仍为已认领 |
| S7-03 RTC 凭证 | 通过 | demo-mode 凭证 `demo:true`，`agoraAppId=demo`，token 不进详情明文 |
| S7-04 开始/加入/结束 | 通过 HTTP+PC | `offered` 非已接通；`joined` 后才 `connected`；agoraUid 须匹配 |
| S7-05 挂断/超时/重启 | 通过 HTTP | 重复 `end` 200；`offered` 超 60 秒读时变为 `timed_out`（服务重启后同样按读时核验） |
| S7-06 TTS | 通过 HTTP+PC | 每设备一行 `accepted`，`heard=false`；提示「未确认现场听到」 |
| S7-07 旧单呼帽号 | 通过代码 | `IntercomRecordServiceImpl` 写入 `request.getHatNumber()`，不再自赋 |

无 `intercom` 能力 409。只读/平台发起通话 403。平台可 TTS。目标是 `deviceId`。

## 单测

- Maven `ruoyi-admin -am`：Tests run **57**，Failures **0**（含 `CallStateMachine`、`WearRoleKeys` 通话/TTS）
- PC Vitest：`calls.test.js` + `events.test.js` **4** passed

## 本机 HTTP

- `siteA_readonly` / `wear_admin` `POST /api/v1/calls` → **403**
- `MH-DEMO-NOV-A`（无对讲）→ **409**
- 值班对 `MH-DEMO-001` SOS：`status=offered` `demo=true`，凭证含 token
- `POST .../joined` → `connected`；`POST .../end` 两次均为 `ended` 200
- 事件状态未因通话变为 `closed`
- TTS `accepted` 且 `heard=false`；只读 TTS **403**
- 人员页发起的会话 60 秒未加入后 `GET /api/v1/calls/{id}` → `timed_out`

## PC

- `siteA_duty` 安全事件：联系 → **待加入** + 演示通道，无「已接通」标签；确认加入后才「已接通」；结束 → 已结束；SOS 仍已认领
- 播报成功提示「平台已受理（未确认现场听到）」
- 人员详情「联系安全帽」、设备详情「联系」均提示待加入，不直接标接通
- `MH-DEMO-NOV-A` / `BL-DEMO-001`：无对讲入口；NOV 可播报
- `siteA_readonly` 事件/人员/设备无联系、播报、确认加入；仍可看会话「已结束」
- 旧 SOS 告警详情无「呼叫已接通」
- 窄视口（约 500px）事件详情「联系/播报」仍可用

## 未做

厂商真实 RTC/TTS 回执（H-HAT-05/06 仍待书面确认）、组呼、通话 WebSocket 推送、旧对讲/TTS 页切流、安全带通信（S8）。demo-mode 不得当生产接通。
