# 通话结束自动返回（2026-09-20）

- 主后端确认整场通话已结束、拒接、超时或离线终止后，自动关闭通话页并返回上一页；无上一页时返回通讯。
- 通话页上打开的参与人员、邀请或画面弹层随通话关闭，不退出其上其他独立页面。
- 仅单个参与者退出且整场通话继续时不关闭；未同步、网络中断等不确定状态不触发关闭。
- 静态分析通过。新增自动返回、弹层清理、多人部分退出和过期状态保护测试，连同现有通话、邀请、紧凑布局共 21 项测试通过。
- 已构建并部署雷电模拟器；实测三人通话从首页进入，安卓挂断确认后自动回到首页，测试设备配置已恢复。
