# 消息页事件筛选样式调整

根据用户 2026-09-21 截图反馈，对事件筛选卡片做局部调整：

- 单行标题配蓝色筛选图标，移除重复的“厂站待办”副标题；右侧保留“筛选 + 下箭头”。
- 四个独立描边按钮改为浅色底的一组分段选项；选中项使用品牌蓝底白字，其余选项使用灰蓝文字。
- 调整标题、按钮字号、内边距和圆角；快捷选项高度由 48 调整为 40，卡片结构更紧凑。
- 保留四项快捷筛选、后台数量、选中状态、高级筛选及条件标签；窄屏或大字号不足以容纳四列时仍自动换成两列。

## 验证

- `flutter test test/wear_event_filters_layout_test.dart test/wear_event_filter_leak_test.dart --reporter expanded`：3 项通过，包含 1.0 / 1.5 字号、四项快捷条件、类型筛选与跨页面筛选隔离。
- `flutter analyze lib/wear/events/events_page.dart`：No issues found。
- `git diff --check` 通过；Debug APK 构建成功。
- 已覆盖安装雷电 emulator-5554。实际检查顶部筛选卡片、高级筛选打开/返回、列表底部“没有更多事件了”、详情页打开及页面返回按钮；返回后保留列表位置，底部导航正常。
- 构建使用主后端 `http://127.0.0.1:18084` 与 `CALL_LAB_ENABLED=true`，未修改后台接口或事件状态。

效果图见 `筛选区调整后.png`。测试与构建日志保存在同目录。
