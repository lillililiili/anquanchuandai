# 核验提交与回顶位置（2026-09-20）

用户要求：通讯、消息页回顶按钮移至右下角；修复非 SOS 事件提交核验后无明确响应、说明被清空。

## 原因与修复

- 核查事件 #169：服务端已有 16:10:01 的 handle 操作，说明“正常”，状态 claimed → handling、版本 3。原前端成功后删除草稿，重建时把输入框同步为空；成功消息又位于页顶，导致页面底部看起来仅刷新。
- 提交后保留核验说明，并记录服务端确认结果；在按钮附近展示结果及后续处理提示，同时显示即时提示。未修改的已提交说明显示“已提交核验”，避免重复提交；修改说明可补充提交。
- 再次进入时读取服务端处置记录恢复已提交说明。重开事件不会沿用上一轮处置的提交标记。
- 空白说明阻止提交；断网、状态冲突保留输入并提示原因。服务端提交成功但刷新失败会明确区分，避免误导用户再次提交。
- 原状态机、权限和正式关闭流程保持不变。普通事件提交核验可进入处置中；需要复核的事件进入待复核。
- 通讯、消息共用回顶按钮改为 bottom: 16、right: 16，位于底部导航上方；滚动超过一个视口显示，返回顶部后隐藏。

## 验证

- 修复前复现测试在 1.0、1.5 字体比例下均发现提交后说明被清空，见 event-submit-red.log。
- 23 项相关回归测试通过，含空白提交、成功保留内容、防重复、补充提交、重新进入恢复、409 冲突、断网、提交后刷新失败、高风险待复核，以及两个列表回顶阈值/定位/隐藏/返回顶部。见 event-submit-regression.log。
- 针对修改文件静态检查通过，见 event-submit-analyze.log。
- Debug APK 构建成功，并通过 adb install -r 安装到雷电 emulator-5554，包名 com.rolling.intelligence_headband。API 地址仍为 http://127.0.0.1:18084。本次未部署后端。
- 实机重新打开事件 #169，说明“正常”恢复，按钮“已提交核验”，表单下展示保存结果和关闭事件指引。实际记录没有为验收重复提交。见 核验内容保留与提交反馈.png。
- 实机消息页按钮位于右下角，底部导航保留；点击后返回带 Logo 的页面顶部，按钮隐藏。见 消息-右下角回顶.png。
- 实机通讯页右下角位置、底部导航已截图确认；点击返回顶部的实机验收期间模拟器被同时进行的通话操作占用，未将此步骤记为通过。通讯回顶交互已通过自动化测试。见 通讯-右下角回顶.png。

现场附件仍是原有本地示例，并未声称图片已经上传。当前工作区含其他任务改动，本次未回退或提交其他任务内容。
