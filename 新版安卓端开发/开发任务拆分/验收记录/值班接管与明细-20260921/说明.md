# S9 值班接管与明细验收

日期：2026-09-21。按本轮明确要求增加 admin 候选、取消交接、管理员接管和值班流水，保持安卓既有页面风格及底部导航。

## 已完成

- 主后端候选接口明确纳入有效的内置 admin，无需为它改角色或写死安卓假数据；安卓显示“若依（admin）”以便识别。管理员也能作为指定接班人确认交接。
- pending 可由管理员或发起人取消，原因必填，取消后记录保留，不改变当前值班人或责任。
- 管理员可把所选厂站值班接管为自己；再次确认并填写原因。记录接管人与时间，接收原负责人仍持有的未关闭事件（含待复核）和未结束作业，取消遗留待接班交接。
- 当前值班卡展示姓名、开始时间、时长；值班明细记录起止时间、时长、接班方式、备注并支持分页。按每厂站一名当前负责人实现，采用厂站行锁和班次 ID 防止旧请求覆盖新值班。
- 旧确认记录可追溯来源。历史交接链不连续时结束时间显示“未记录”、时长“无法计算”，不补造缺失时段。
- 取消和接管输入框由表单组件管理生命周期，退出弹窗没有提前销毁控制器。

## 验证

- Java 20 项通过（DutyManagementTest、DutyLedgerTest、EventStateMachineTest、WorkTaskStateMachineTest）：候选、角色/厂站隔离、取消权限、冲突、过期班次拒绝、数据库时间格式、既有状态流转。
- Flutter 4 项通过：管理员取消/接管/明细展示，正常接班、设置/首页返回、非值班只读、大字体布局；相关源码 analyze 无问题。
- Docker 实际接口 10 项通过，见 duty-api-results.json。专用临时厂站验证 admin 接班、管理员接管、责任实际转移、发起人取消、取消留痕、并发取消/确认一胜一拒、分页与跨站拒绝；临时厂站及相关数据全部清理。
- 原接班人复现脚本从 `adminPresent=false` 变为 true；A 站接口现返回 8 名合格人员。
- 雷电验证页面顶部、当前值班、明细及页面底部；打开接管/取消原因弹窗并返回，保留底部四栏。应用进程日志未发现 Flutter 断言、未处理异常或布局溢出。
- 实机只打开/取消操作弹窗，没有替用户提交真实厂站的接管或取消；原 A/B 站 6 条交接状态未改动，当前 A 站仍为周明。

## 部署

- Docker 已执行 V013、V014 增量迁移并运行新版后端，健康状态 healthy。迁移前原交接表备份：`C:/melhat-runtime/duty-before.sql`。
- Maven 增量 jar 曾复用旧依赖包；最终使用 `-Dmaven.jar.forceCreation=true`，并核对编译字节码和实际接口，防止仅源码更新。
- APK 已覆盖安装到雷电 emulator-5554，原登录状态保留。包名 `com.rolling.intelligence_headband`，版本 1.3.0，最后安装时间 15:01:45。
- APK：`C:/melhat-runtime/wearable-android/build/app/outputs/flutter-apk/app-debug.apk`。
- SHA256：`B2788E3E04628ABB2CEDA527EF4F8F74007F8F6692365A6A5D3CF48294B06DA9`。
- 主后端仍通过 `http://10.143.40.195:18084` 对接，安卓未直连模拟设备。

截图、回归日志及本机接口脚本在本目录。接口更新见 `docs/contracts/s9-work.md` 第 5 节。未提交 Git，未覆盖其他并行工作。
