# 事件与通讯直接筛选验收

日期：2026-09-21。对应用户反馈：此前筛选层级过多、点击次数多；重新优化两个页面并统一风格。延续现有任务，本次不改变事件处置流程。

## 本次结果

- 两页复用 `InlineFilters`，状态、设备等常用条件直接呈现，点选即筛选，取消总开关及“应用”步骤。
- 保留多选：同组 OR、跨组 AND；“不限”清空一组，“重置”清空全部。状态“未关闭”便捷组合由首次具体状态选择替换。
- 长类型、班组、作业横向浏览，箭头原位展开；超过 8 个选项可搜索。展开后连续选择不会自动收起，已选摘要保留。
- 选择反馈立即显示，300ms 合并查询；保留旧结果并显示加载，沿用过期响应保护。自动查询不主动滚回顶部，勾选图标预留空间，避免相邻选项移动。
- 具体告警代码及名称继续读取主后端，安卓没有新增直连模拟设备；原多选查询接口及服务端分页不变。

## 验证

- 相关 Flutter 回归 32 项通过，覆盖连续选择防抖、缓存恢复、过滤组合、筛选隔离、通讯多选与滚动；最后的摘要/排序调整后，相关 UI 用例 8 项再次通过。
- Flutter analyze：No issues found。`git diff --check` 通过。
- 雷电原生 APK：事件默认 45 条，单击待认领为 32 条；组合待认领、跌落/撞击、安全帽为 2 条。类型选中后展开区保持打开。
- 通讯筛选：主平台当时没有在线安全帽；在线+安全帽显示 0 条，不伪造在线人员。补选离线后显示 43 人，筛选区位置保持不动。重置恢复完整名单。
- 底部切换通讯再回消息，事件条件及滚动位置保留；本轮应用进程日志未发现 Flutter 断言、未处理异常或 RenderFlex 溢出。
- 两页初始状态、展开状态、组合筛选截图及测试/构建日志保存在本目录。

## 安装

- Debug APK 已覆盖安装到雷电 `emulator-5554`；包名 `com.rolling.intelligence_headband`，版本 1.3.0，安装更新时间 2026-09-21 11:01:51。
- APK：`C:/melhat-runtime/wearable-android/build/app/outputs/flutter-apk/app-debug.apk`。
- SHA256：`38C328E89DF7E6CD5BAA108C3304C5A666F856B41F32312C8FCD952D48546BFC`。
- 主后端地址 `http://10.143.40.195:18084`；本次未重启或改动后端服务。

参考资料及取舍见同目录《筛选交互资料与决策.md》。
