# 消息列表分流、异常核验02与SOS协助04

2026-09-17。本轮按用户明确要求制作两个子页面，状态为开发自验通过、待用户视觉审核；不代表实时地图、协助组、相册上传及硬件联调完成。

## 简单审核预览

- 橙黄色异常图标：非SOS事件进入02异常核验页。
- 红色感叹号图标：type=sos进入04 SOS协助页；演示事件显示SOS演练，非演示显示SOS求救。
- 两页均不显示底栏，返回恢复原列表；其他主页面和四栏样式不变。
- 认领、转交、关闭、重开、任务关联、原处置表单及时间线保留在“事件资料与原处置功能”展开区。

[事件列表](01-事件列表.png) · [异常核验首屏](02-异常核验.png) · [异常说明与附件](03-异常下部.png) · [SOS首屏与示意地图](04-SOS协助.png) · [SOS协助操作](05-SOS下部.png)

## 页面布局和已有功能的衔接

| 区域 | 本轮实现与保留 |
| --- | --- |
| 02页头 | 浅色天空、手持平板安全员、黄色告警标识；沿用系统ROLLING和返回入口 |
| 异常摘要 | 原事件类型/ID、人员/设备快照、关联作业、发生时间、真实状态；不硬编码预览姓名、票号和状态 |
| 原安监/来源卡 | 只读显示后端sourceEventId；来源编号存在不代表原安监同步已联调，未关联时明确提示 |
| 现场核验说明 | 沿用原EventDraft.handleComment；保存正文到原账号/厂站草稿存储；提交仍调用原handle，带真实事件ID及version |
| 提交权限 | 原EventPolicy继续决定能否handle；未认领或当前角色无权限时禁用并说明，原认领入口保留在展开区 |
| 现场附件 | 最多4张，本页可添加示例、预览、删除；尚无系统选图、真实文件草稿和上传；不会把示例照片混入真实事件请求 |
| 04红色SOS区 | 按事件demo区分演练/真实求救，人员和设备来自当前事件快照 |
| 地图 | 复用plant_map.jpg作为假地图，标注“厂区示意 · 非实时地图”；图片地名和标记均为示意 |
| 位置来源 | 真实SN与事件时间；无坐标显示位置未知。有坐标也明确为事件快照，不冒充实时定位 |
| 协助组、加入/结束 | 组服务未接入；弹层可体验本地加入/结束，或打开原通讯能力；不建立真实组会话、不自动拨号、不关闭事件 |
| 原通信能力 | 携带eventId、deviceId、personId进入既有通讯页面，继续其真实能力/权限约束 |
| 原事件功能 | 原详情、原动作按钮、版本冲突提示、时间线与设备位置/任务信息全部保留；高风险复核/结案规则未修改 |

## 实现范围

生产代码仅修改 `android代码/melhat_android-main/lib/wear/app.dart`、`lib/wear/events/events_page.dart`，新增 `lib/wear/events/event_reference_view.dart`。后两个路径相对安卓工程根。

事件列表改动仅图标分类与点击进入子页；筛选、分页、列表信息和查询参数保留。路由仍为 `/events?eventId=...`。壳层仅对此详情隐藏底栏，作业详情原隐藏规则继续保留。EventController、EventPolicy、EventGateway、事件模型和后端均未改写。

新增 `verify_reference_hero.png` 与 `sos_reference_hero.png`，同时放入安卓 `assets/field-brand/preview/` 和 `新版安卓端开发/UI素材/背景/`，已在真实页面引用。由imagegen分别生成核验平板人物、SOS对讲机人物背景；没有覆盖原素材。地图通过界面裁切显示原图，未修改原地图文件。

## 验证及实际边界

- 41项回归测试通过，见[test.log](test.log)，包括原权限策略、409保草稿、防重复提交、请求版本与ID、两个事件分流、正文草稿返回恢复、普通及1.5倍字号、示例照片增删、SOS操作无事件写请求、作业详情及四栏/我的既有功能。
- 原SOS处置表单测试改为从新子页展开原功能后执行，仍验证重启恢复草稿、键盘/窄屏、真实提交参数及防重；没有用新UI替换掉原业务断言。
- 针对本轮3个生产文件和2个测试文件静态分析通过，见[analyze.log](analyze.log)。
- 雷电使用后端演示数据围栏事件#2、SOS事件#1检查界面。未为截图提交真实认领、处置或通话。
- 参考图中的示例数据、人物和字体无法作为真实业务字段；短屏滚动，长姓名/设备编号允许换行。视觉相似度待用户审核，不将测试通过称为逐像素验收。

## 任务进度

T25/T26/T28/T29/T30的本轮UI交付待用户审核；T23仅分类图标和入口、T24仅原功能融入、T27仅本地示例附件，三项标部分完成。有效48项中15项待用户审核、6项部分完成、27项未开始。T01仍取消，其他任务文档没有删除。

## 回退

修改前代码、测试、受影响任务文档及before.apk在项目根相对路径 `垃圾桶/2026-09-17-异常核验与SOS修改前/`，镜像保留相对路径。新增文件、原文件备份和SHA256见[恢复与新增文件清单](恢复与新增文件清单.json)。只恢复本次范围，不恢复或删除其他任务成果。

## 部署

构建命令：`flutter build apk --debug --no-pub --target-platform android-x64 --dart-define=API_BASE_URL=http://127.0.0.1:18084`，构建日志见[build.log](build.log)。雷电设备emulator-5554，包名com.rolling.intelligence_headband，保留1080×1920原分辨率和密度。ADB reverse将18084映射到当前Docker服务。

2026-09-17 17:24，ADB覆盖安装返回Success，应用成功启动。安装后检查异常页、SOS页和列表返回；已保存本目录截图。最终APK SHA256：59a92440953b36b4dc85186d66f365bd632d13042e7d1c0de672a5e53ae03c68。本地APK与雷电已安装base.apk的SHA256一致。

