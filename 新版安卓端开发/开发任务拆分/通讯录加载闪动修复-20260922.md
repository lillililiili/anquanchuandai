# 通讯录加载闪动修复（2026-09-22）

## 目标与范围

用户截图：通讯页在正常列表与“通讯数据加载失败 / 仅管理员可查看全站作业”之间循环闪动。通讯录应显示当前厂站所有人的通讯情况；不能由作业列表的权限错误阻断通讯录。

保持既有布局、Logo、底部导航、筛选、星标、群聊和播报。修复限安卓与必要主后端；5188 联调平台全目录保持只读，禁止构建部署。保留作业组隔离和管理员管理权限。

## 分工与状态

- 消息页面ui设计：通讯页面、联系人数据加载和专用回归；先复现再修复。
- 通讯页面ui设计：主后端和安卓权限诊断已发现普通账号通讯入口及人员设备读取被额外阻断；获派 `app.dart`、`session.dart`、`InspectionAccessConfig.java`、`SiteAccessService.java` 及权限专项测试，只恢复通讯录只读访问，保留原有管理和通讯写权限。
- 我的页面ui设计：构建阻塞只读诊断。
- 总指挥：集成、记录、构建及最终部署。用户负责页面/业务验收。
- 状态：诊断中；尚未变更运行环境。

## 证据与检查点

- 截图：`C:/Users/qiyue/AppData/Local/Temp/codex-clipboard-49e67801-85db-45ac-8400-617a238bb91b.png`。
- 初读请求入口：`ContactRoster.load` 同时请求人员、设备和 `/api/v1/work-tasks`；具体因果待回归复现确认。
- 运行环境可达：雷电 `emulator-5554`；`melhat-backend` 和联调容器均处于 healthy。容器健康不代表业务权限正确。
- 5188 文件哈希基线：`C:/melhat-runtime/communications-protected-baseline-20260922.json`，用于本轮结束比对。
- 待补：最小红测试、根因、改动文件、检查结果、部署源码/产物与用户验收状态。
- 构建诊断：两套缓存的 wakelock 1.7.0 源码和生成 Kotlin 哈希相同；旧增量记录仍指向 1.6.0。当前解析文件使用默认 Pub Cache，计划在回归进程退出后定向 clean 插件并禁用增量编译验证，不升级依赖、不手改生成代码。

## 续办检查点（2026-09-22，通讯加载异常与雷电部署）

- 接管时先核对相关任务，均已中断或未加载；保留已有工作树，接续已有通讯修复，不再派工。
- 使用 HEAD 中原 ContactRoster 的隔离副本运行现有回归，稳定失败并输出“仅管理员可以查看全站作业”。原件与复现脚本仅在 `C:/melhat-runtime/comms-flicker-release-20260922/`，没有回退工作区。
- 已核对修复：普通账号读取 `/work-tasks/mine`，管理员使用全站作业；作业筛选 403/404/服务异常不会丢弃本站联系人。后台刷新保留名单并将陈旧在线状态标为未知；通话历史 403 不阻断通讯录。
- 本轮补强通讯回归：断言实际请求 mine 接口和筛选不可用状态；历史权限测试确保真的发出历史请求；群聊、视频测试夹具使用有通讯操作权限的管理员，不放宽生产权限。
- 运行后端实测（普通账号、厂站 1）：人员 200/33 条，设备 200/42 条，我的作业 200，全站作业 403。现有后端已支持此次安卓修复所需接口，本轮不重启、不部署主后端；此前后端权限源码改动仍留在工作区。
- 测试：通讯默认模式 68 项通过、4 项条件跳过，补强断言修正后对应 5 项全部通过；联调模式通讯回归 15 项全部通过。初轮把原生通话接口测试放入联调模式造成接口预期不符，已按各自模式检查，未据此改动生产接口。
- 前序留下的独立 Docker 构建已完成，27 项权限/事件测试通过；没有替换运行服务。
- 保护核对：5188 基线 39 个文件 SHA256 全部一致。
- 当前：安卓打包中，已定向清理 wakelock 插件构建缓存并关闭 Kotlin 增量编译；新出现重复类检查失败，待读取完整构建报告处理。尚未安装新 APK。
- 证据目录：`C:/melhat-runtime/comms-flicker-release-20260922/`，包含 baseline-red.log、refresh-green.log、flutter-native-tests.log、flutter-lab-green.log、api-before.json、source-hashes.json、android-build.log。

- 收尾更新：默认模式完整重跑为 69 项通过、4 项条件跳过；联调专项 15 项通过；8 个相关源码/测试文件静态分析无问题。构建失败报告确认是离线缓存缺少 collection-ktx 1.4.2、kotlin-stdlib-jdk7/jdk8 1.8.0，并非重复类冲突；联网补齐后重复类检查及 wakelock Kotlin 编译已通过。主业务源码构建前后哈希一致。

## 最终部署结果（2026-09-22 12:02）

- 安卓构建成功：`android-build-final.log`，Gradle `:app:assembleDebug`；API_BASE_URL=`http://10.143.40.195:18084`，CALL_LAB_ENABLED=true。仅用构建参数关闭 Kotlin 增量并使用进程内编译，没有升级依赖、修改插件生成代码或项目 Gradle 配置。
- 产物：`C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk`；SHA256 保存在证据目录 `apk-sha256.json`。源码基线为现有分支 HEAD 063bfdf 加保留的工作树改动，相关业务文件指纹见 source-hashes.json。
- 雷电 emulator-5554 覆盖安装返回 Success，应用 `com.rolling.intelligence_headband`，版本 1.3.0 / 7，模拟器记录 lastUpdateTime=2026-09-22 11:59:27，已重新启动。没有清除用户数据。
- 旧进程安装前最后一次 UI 抓取（after-install.xml）仍出现“通讯数据加载失败 / 仅管理员可以查看全站作业”，进一步确认真实复现。该抓取与安装并行，不能误称新 APK 的结果。
- 新进程页面实测：保留原登录账号 qa_chen（班组长）、演示厂站 A；通讯列表正常显示 72 项。检查下拉刷新、滚动到底部、回到顶部、切换我的页面、进入通讯服务子页并返回，再回通讯页，未出现原错误。最终停留在通讯页顶部。
- 验证截图：comms-loaded.png、comms-bottom.png、comms-final.png；结构证据：comms-bottom.xml、service-child.xml、comms-final.xml。XML 自动断言确认返回后存在联系对象筛选且不含原加载失败/全站作业权限提示。
- 页面上的离线、未知和联调标签按接口原状展示，本轮未发起呼叫、播报或修改设备在线状态；未修改、重启或部署 5188 平台，未部署主后端，未提交/推送。以上为必要回归和部署检查，最终业务验收由用户完成。

## 用户要求再次打包部署（2026-09-22 13:36）

- 按当前工作树重新执行 `:app:assembleDebug`，归档上次 APK 后重新生成产物；构建成功（1 分钟）。沿用 API_BASE_URL=http://10.143.40.195:18084、CALL_LAB_ENABLED=true、Debug 1.3.0+7。
- 构建前后 lib 与 Android app/src 文件 SHA256 核对一致。未修改业务源码、后端或 5188 独立平台。
- 雷电 emulator-5554 执行 `adb install -r` 返回 Success，lastUpdateTime=2026-09-22 13:36:08；保留应用数据并重新启动，首页正常展示现有登录用户和厂站。
- 证据：`C:/melhat-runtime/android-redeploy-20260922-133412/` 下 build.log、install.log、installed-version.txt、source-hashes.json、apk-sha256.json、startup.png；APK 为 `C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk`。
- 本次重装后进入通讯页，界面结构检查确认联系人筛选和名单正常显示，未出现原加载错误。

## 用户要求再次推送部署安卓端（2026-09-22 13:56）

- 本轮“推送部署”按连续上下文执行为重新打包并安装至雷电，不执行 Git 提交/推送。
- 当前工作树基线 HEAD 063bfdf；相对 13:36 部署包含 contact_action_bar.dart、events_page.dart、queries/tasks.dart 三个源码文件的新改动，均原样打包，未另改业务源码。构建前后源码指纹一致。
- Gradle :app:assembleDebug 成功（30 秒），Debug 1.3.0+7，API_BASE_URL=http://10.143.40.195:18084，CALL_LAB_ENABLED=true。
- emulator-5554 覆盖安装返回 Success；lastUpdateTime=2026-09-22 13:56:24。已重新启动，进程存活，首页和原登录账号/厂站正常显示，应用数据保留。本轮只完成必要编译和启动检查，新功能业务验收由用户完成。
- 证据目录：C:/melhat-runtime/android-redeploy-20260922-135436/，包含 build.log、install.log、installed-version.txt、source-hashes.json、apk-sha256.json、startup.xml、startup.png。
- 产物：C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk。没有部署主后端或操作 5188 独立平台。

## 用户要求再次推送部署安卓端（2026-09-22 15:12）

- 基线 HEAD 063bfdf 加当前工作树；相对 13:56 产物包含 11 个事件/消息/巡检作业源码文件的新改动，未额外修改业务代码。构建前后已记录文件 SHA256 一致。
- Gradle :app:assembleDebug 成功（1 分 44 秒），归档旧 APK 并重新生成 Debug 1.3.0+7。参数 API_BASE_URL=http://10.143.40.195:18084、CALL_LAB_ENABLED=true。
- 雷电 emulator-5554 覆盖安装返回 Success，lastUpdateTime=2026-09-22 15:12:52；重新启动后进程存活，首页正常显示原登录用户、厂站和作业，保留应用数据。
- 证据目录：C:/melhat-runtime/android-redeploy-20260922-150954/，含 build.log、install.log、installed-version.txt、source-hashes.json、apk-sha256.json、startup.xml、startup.png。产物：C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk。
- 此轮范围为安卓重新打包安装及启动检查；不部署后端、不操作 5188 平台、不执行 Git 提交/推送，业务验收由用户完成。

## 用户要求再次推送部署安卓端（2026-09-22 15:42）

- 基线 HEAD 063bfdf 加当前工作树；相对 15:12 部署包含 communications_page.dart、contact_action_bar.dart、events_page.dart、queries/tasks.dart、queries/workbench.dart 五个文件的新改动，原样打包。构建前后源码指纹一致，未额外修改业务代码。
- Gradle :app:assembleDebug 成功（2 分 16 秒），重新生成 Debug 1.3.0+7；参数 API_BASE_URL=http://10.143.40.195:18084、CALL_LAB_ENABLED=true。
- emulator-5554 覆盖安装返回 Success，lastUpdateTime=2026-09-22 15:42:54。已重启应用并确认进程存活、首页显示正常；原登录与应用数据保留。
- 证据：C:/melhat-runtime/android-redeploy-20260922-153921/，包含 build.log、install.log、installed-version.txt、source-hashes.json、apk-sha256.json、startup.xml、startup.png。APK：C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk。
- 本轮仅重新打包、模拟器安装和启动检查；没有部署后端、操作 5188 平台或 Git 提交/推送。最新业务改动由用户验收。

## 用户要求再次打包推送并部署安卓端（2026-09-23 09:07）

- 按连续上下文将“推送”执行为 APK 推送安装至雷电。基线 HEAD `1c76b69` 加当前工作树，包括 `event_reference_view.dart` 的未提交改动；构建前后 lib、Android app/src、pubspec.yaml/lock 指纹一致，未另改业务源码。
- Gradle `:app:assembleDebug` 构建成功（1 分 42 秒），Debug 1.3.0+7；API_BASE_URL=http://10.143.40.195:18084、CALL_LAB_ENABLED=true，沿用已有 Kotlin 构建参数。
- 雷电 emulator-5554 覆盖安装返回 Success，lastUpdateTime=2026-09-23 09:06:40。已重启并确认进程存活，首页顶部、当前作业、SOS 入口和底部四栏正常显示，原登录与应用数据保留。此次为打包、安装和启动检查，业务验收由用户完成。
- 证据目录：`C:/melhat-runtime/android-redeploy-20260923-090430/`，含 build.log、install.log、installed-version.txt、source-hashes.json、apk-sha256.json、startup.xml、startup.png 和上次 APK 备份。新 APK：`C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk`。
- 未部署主后端、未操作 5188 独立平台，未执行 Git 提交/推送。

## 用户要求打包推送并部署安卓端（2026-09-23 10:15）

- 基线 HEAD `1c76b69` 加当前工作树，原样构建；lib、Android app/src、pubspec.yaml/lock 构建前后哈希一致。未另改业务代码。
- Gradle `:app:assembleDebug` 成功（1 分 25 秒），Debug 1.3.0+7，API_BASE_URL=http://10.143.40.195:18084、CALL_LAB_ENABLED=true，沿用既有构建参数。
- 雷电 emulator-5554 执行覆盖安装返回 Success；lastUpdateTime=2026-09-23 10:14:47。重启后进程存活，等待加载完毕重新抓取 UI 成功，首页顶部、当前作业、SOS 入口和底部导航正常，保留原登录与应用数据。首次启动过早抓取失败的旧 XML 不作为验证证据，证据文件已由成功的新抓取覆盖。
- 证据：`C:/melhat-runtime/android-redeploy-20260923-101259/`，含构建/安装日志、版本记录、源码与 APK 哈希、startup.xml/png 及旧包备份。APK：`C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk`。
- 本轮“推送”为 APK 推送安装；未执行 Git 提交/推送、后端部署或 5188 平台操作。业务验收由用户完成。

## 用户要求打包推送并部署安卓端（2026-09-24 08:51）

- 基线 HEAD `a7ae39b` 加当前工作树，原样打包最新安卓源码；lib、Android app/src、pubspec.yaml/lock 构建前后哈希一致，未额外修改业务代码。
- Gradle `:app:assembleDebug` 成功（2 分 33 秒），Debug 1.3.0+7，API_BASE_URL=http://10.143.40.195:18084、CALL_LAB_ENABLED=true，沿用既有 Kotlin 构建参数。
- 雷电 emulator-5554 覆盖安装返回 Success，重启后进程存活，首页顶部、作业卡、SOS 入口与底部导航显示正常，原登录与应用数据保留。模拟器系统时钟落后于宿主机，安装记录显示 2026-09-23 17:35:36；已直接核对设备 base.apk SHA256 与本轮构建包完全一致，不能以该旧日期误判未安装新包。SHA256：`64147cd3be0ac42fd5fd7ade08d431fe51158e2922f039a7d216a736c8979a92`。
- 证据：`C:/melhat-runtime/android-redeploy-20260924-084801/`，含 build.log、install.log、installed-version.txt、installed-sha256.txt、source-hashes.json、apk-sha256.json、startup.xml/png 及上次 APK 备份。产物：`C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk`。
- 运行依赖限制已告知用户：当前新安卓复核调用 review 接口，主后端实际仍为 9/23 包（SHA256 `9042198d42e467f3c58c22f9d0fe034a41b43465ac602142889f0dc24fa9c121`），未包含 9/24 平台核验/复核边界改动。首页启动检查不代表新复核功能已可用，需主后端后续同步部署。本轮仅按请求安装安卓，未修改/部署后端或 5188 平台，未代为操作真实事件，未执行 Git 提交/推送。

### 通讯列表仅显示联系人，事件入口正确切换通讯（2026-09-24）

- 用户指定两项：通讯名单不再按人员及设备重复列项；事件详情“联系现场”应切换底部通讯并筛选对应人员。延续无需打包部署。
- 安卓和网页展示源码同步小范围调整 `communications_page.dart`、`events_page.dart`、`app.dart`。名单仅用人员 ID 生成联系人，移除独立设备行与设备类型筛选，搜索改为“搜索联系人”。设备仍用于后台呼叫、在线状态和播报能力判断，保留从装备详情明确指定通话设备的既有行为。
- 事件入口统一使用分支切换 go，修复原 push 导致通讯页面叠在消息分支、底部仍高亮消息的问题。传递人员 ID 和一次性 filterRequest；通讯页按 ID 精确筛选并勾选目标，清除冲突搜索／筛选，保留底部导航与三项通讯操作。支持清除联系人筛选；后台刷新不重新套用已清除的筛选；再次点击同一事件入口可重新定位。
- 历史仅携带事件 ID 的入口读取事件人员；人员身份优先于设备当前领用人，避免设备换绑后定位到其他人。未添加自动拨号或广播。
- 修复前新增组件测试复现两项：设备行仍显示；进入通讯后 NavigationBar.selectedIndex 仍为 2。修复后覆盖同名人员按 ID 区分、旧搜索清除、反复进入、清除筛选后刷新不恢复、顶部联系人与底部操作可达。安卓默认模式相关测试 19 项通过（2 项联调用例条件跳过），联调模式对应群聊与视频用例 2 项通过；网页展示新增 2 项通过。两套源码限定 Dart 静态检查无问题，限定 diff 检查通过。
- 未改后端／业务数据或 5188 平台，未打包、部署、安装；保留工作树其他任务修改。用户验收仍需后续统一安装／发布。

## 再次打包推送并部署安卓端（2026-09-24 09:31）

- 用户再次要求安装当前安卓源码，基线 HEAD `a7ae39b` 加工作树。相对 08:51 部署新增 app.dart、communications_page.dart、events_page.dart 改动，原样纳入；构建前后已有源码指纹一致。
- Gradle assembleDebug 成功（1 分 28 秒），Debug 1.3.0+7，API_BASE_URL=http://10.143.40.195:18084、CALL_LAB_ENABLED=true。雷电 emulator-5554 覆盖安装 Success，保留原数据，已重新启动。
- 设备内 base.apk 哈希与本次构建产物一致：`d1de0f87ded7d5ffdbf165ca7de662108874be55fa5c7dd7e0edbfe03cdc158b`。模拟器时钟仍落后于宿主机，系统安装时间记录为 2026-09-23 18:15:40，不据此判断产物新旧。
- 启动检查：MainActivity 在前台、进程存活；实际截图显示原登录用户、厂站、首页作业及底部四栏正常。UIAutomator 两次返回 null root，未取得 XML；改用新抓取截图进行可视检查，未把旧 XML 当本次结果。
- 证据：`C:/melhat-runtime/android-redeploy-20260924-092857/`，包含 build.log、install.log、installed-version.txt、installed-sha256.txt、source-hashes.json、apk-sha256.json、startup.png 和旧包备份。APK：`C:/melhat-runtime/wearable-android/build/app/outputs/apk/debug/app-debug.apk`。
- 后端运行 JAR 哈希仍为 `9042198d42e467f3c58c22f9d0fe034a41b43465ac602142889f0dc24fa9c121`，上一节记录的新平台复核依赖仍待后端同步部署。此次仅安装安卓，无后端/5188 平台操作，无 Git 提交/推送。新业务由用户验收。
