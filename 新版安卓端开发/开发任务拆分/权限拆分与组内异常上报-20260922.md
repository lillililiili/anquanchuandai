# 权限拆分与组内异常上报（2026-09-22）

## 装备详情跳转与首页手动 SOS（2026-09-22，源码完成、未打包部署）

- 最新用户要求：我的装备点击设备进入详情；首页增加可填写信息的手动 SOS，仅报警本人和管理员可见，跳过一级审查直接进入管理员审批。用户再次明确“继续，无需打包”。
- 装备根因：安卓非管理员路由白名单拦截 `/devices/:id`，且详情同时请求无权读取的设备完整领用历史。`app.dart` 放行数字设备详情路径，`queries/devices.dart` 普通账号仅读取现有详情接口，管理员保留历史及管理能力；后端现有厂站只读范围不扩大。
- 首页当前作业卡下新增红色“手动 SOS 报警”入口（`queries/workbench.dart`），`events/manual_sos_page.dart` 填写位置/说明，校验、提交中禁用、失败保留输入、重试使用同一请求标识；真实提交成功后刷新首页/消息并进入事件详情。既有详情页头与说明针对手动来源显示管理员审批，操作记录新增手动 SOS 文案；保留详情原有导航规则和返回后的底部导航。
- 后端新增 `ManualSosService` 与 `POST /api/v1/events/manual-sos`；身份/厂站由会话确定，不信任请求中的用户、状态和厂站。新事件为真实 `sos/emergency/pending_review`，创建审计记录并沿用事务提交后的通知。数据库唯一来源键配合用户/厂站/请求标识防止重试重复建单，重复请求修改内容返回409。无绑定设备或巡检组的授权账号仍可报警，不伪造设备/GPS信息。
- `EventAccessService` 对手动来源单独按 `reporter_user_id` 或管理员限定列表/详情/数量/通知，同组其他人员不获得可见权限；历史普通设备事件权限不变。`EventCommandService` 手动来源无需现场上报即可审批，沿用管理员权限、版本检查和禁止本人审批；手动事件重开仍进入待管理员审批。通用采集入口禁止伪造保留的手动来源。
- 数据依赖：新增 `sql/migrations/V020__manual_sos.sql` 和事件模型/DTO 的 `reporterUserId`。**脚本未执行；以后部署必须先执行 V020，再更新主后端及安卓，不能只更新APK即宣称已生效。**
- 检查：安卓12项通过（装备打开/返回、表单校验、失败重试、重复点击禁用、正常/1.5倍字体、审批权限及巡检回归），10个Dart文件静态分析无问题；后端独立Docker源码副本执行 Maven `test`，53项通过，含H2实际范围查询和通知隔离、幂等/身份防伪、直接审批/重开。仅编译测试，没有执行 package 或安装。
- 日志：`C:/melhat-runtime/manual-sos-android-tests-20260922.log`、`manual-sos-android-analyze-20260922.log`、`manual-sos-backend-tests-20260922.log`。新增测试 `wear_manual_sos_test.dart`、`ManualSosTest.java`，更新装备、事件范围和流程回归。测试字号场景使用独立账号避免跨测试复用静态写入队列；临时诊断日志已移除。
- 未打包、部署、重启主后端、执行业务库迁移、安装APK、提交或推送；5188平台保持只读。待后续统一部署后由用户完成实机验收。

依据本次用户明确要求修改；历史设计中的认领、转交、普通值班员关闭事件流程不再适用。本任务仅修改代码及测试，没有执行提交、打包部署、数据库迁移或模拟器安装。

## 已实现

- 管理员身份沿用 `admin`、`wear_platform_admin` 或后端管理员标识。其他角色（包括旧值班员、组长、复核员）只有巡检、上报及相关个人功能；安卓入口、路由和后端 `/api/v1` 接口同时限制。
- 每个作业任务的成员、负责人及监护人构成作业组。普通用户只能查看当前未结束作业组已关联的异常。未关联或关联不明的异常不向普通用户扩大开放，由管理员处理。
- 移除认领、转交操作。组内任意成员直接填写异常原因；首次成功上报后结束组内处理，状态进入管理员待复核，普通界面显示“已上报 · 已完成”，从普通用户待办中移出。管理员填写复核结论后关闭；保留版本校验，重复提交或并发处理返回冲突并保留草稿。
- 低电量、没电、长时间静止等已识别且非紧急的设备提醒只对事件发生时的佩戴者可见，点击“确认并结束”，无需填写原因，也不通知管理员。未知类别、未挂钩、围栏及紧急告警仍需上报，不能仅因低严重度而直接关闭。
- 列表、筛选项、详情、操作记录、消息数量、首页摘要、作业内异常及通知收件人共用事件范围规则；旧待复核状态的本人设备提醒仍可直接确认。
- 保留原页面风格、Logo、底部导航和按钮比例；普通账号停用通讯及管理入口。现有巡检记录与报告接口继续使用。

## 验证

- 安卓两组回归测试合计覆盖 45 个不同测试，全部通过：权限、直接上报、提醒确认、管理员复核弹窗、草稿、网络失败、并发冲突、筛选、位置、顶部/底部、父子页面返回和放大字体。
- 补充管理员通讯入口与筛选回归 12 项，全部通过；仅将共享设备跳转测试夹具调整为管理员身份，未放宽普通账号权限。安卓相关验证合计 57 项。
- 后端 24 个测试全部通过：`InspectionEventScopeTest`（7）、`InspectionEventWorkflowTest`（6）、`EventStateMachineTest`（5）、`EventFilterTest`（6）。范围测试使用 H2 实际执行 SQL；测试库仅用于测试，不接触业务数据库。
- 后端在独立 Docker 容器的临时目录复制源码后编译和测试，未替换运行服务；安卓通过组件测试验收，未安装到模拟器。

## 协作边界

工作区同时存在其他任务的巡检 UI 和通讯改动，均保留。本次新增的服务端设备提醒分类使用事件快照的明确代码/名称白名单；后续接入新增设备告警名称时需同步补充分类及测试。

## 消息页告警可见范围复核（2026-09-22）

- 用户本轮要求：非管理员仅看到当前作业组产生的安全告警；管理员看到全部安全告警。用户补充确认：设备提醒继续仅本人可见，不向同组成员或其他管理员开放。顶部厂站选择继续生效。
- 核对工作区已有 `EventAccessService`：作业成员、负责人及监护人按未结束作业（ready/in_progress/paused）授权；没有作业或关联不明的安全告警不向普通账号开放。列表、筛选选项、详情、操作记录、首页摘要、数量、作业详情内事件及通知均已有对应范围校验，本轮保留该实现。
- 本轮安卓修改仅 `lib/wear/events/events_page.dart` 的普通账号数量文案，由“全站未关闭”改为“我的待办”；页头已有“当前作业组异常 · 本人设备提醒”。管理员文案、布局与功能保持现状。
- 新增 `test/wear_event_scope_ui_test.dart` 覆盖普通角色与两类管理员的范围提示、服务端待办数量和厂站请求头。扩充 `InspectionEventScopeTest.java`，实际执行 H2 SQL 验证负责人/监护人、失效人员、跨厂站错误关联、数量以及使用全部状态/指定其他作业筛选不能越权。
- 运行版本差异：只读检查当前 `melhat-backend` 的 `/workspace/ruoyi-admin/target/fdc-admin.jar`，其中 `EventQueryService` 没有 `eventAccess` 字段，且内嵌 `ruoyi-system-3.8.5.jar` 不含 `EventAccessService`。因此当前运行包尚未包含工作区的作业组隔离，不能仅更新 APK 后宣称生效。
- 本轮未部署、未安装 APK、未迁移数据库、未提交推送；5188 联调平台未修改。后续由总指挥集成部署包含权限实现的主后端与安卓版本，用户完成实际账号验收。
- 检查完成：安卓相关回归 22 项通过，改动页面与新增测试静态分析无问题；后端 29 项专项测试通过（范围 12、流程 6、状态机 5、筛选 6），编译成功。证据：`C:/melhat-runtime/event-scope-android-tests-20260922.log`、`C:/melhat-runtime/event-scope-android-analyze-20260922.log`、`C:/melhat-runtime/event-scope-backend-tests-20260922.log`；运行包检查：`C:/melhat-runtime/event-scope-running-package-20260922.log`。后端测试在独立容器的源码副本执行，不替换运行服务。页面上下部、父子页返回及放大字体由既有组件回归覆盖，实际模拟器与账号验收尚未进行。

## 按所属所有作业组的人员过滤告警（2026-09-22，后续明确需求）

- 用户以 13:22 消息页和 13:26“我的任务”截图明确：事件列表显示当前账号所属所有作业组中包含人员的告警，管理员可查看全部；图 2 仅供参考，不修改作业组列表。此条替代前文“仅未结束作业、必须匹配事件 task_id”的规则。设备提醒沿用用户此前确认的仅本人可见。
- `EventAccessService.scope` 改为按事件 `person_id` 匹配所属作业组的成员名单（`wear_work_task_member`）。账号所属组仍由成员、负责人或监护人关系确定；不限定组状态，与“我的任务”包含全部组的范围一致。通过 `EXISTS` 汇总人员，重叠成员不会导致事件重复。
- 同组人员的告警即使未绑定任务、匹配不明或绑定其他任务，也按人员归属可读；反之，外组人员的事件即使绑定本组任务，也不可读。事件厂站必须与授权组厂站一致；原有厂站选择及授权检查继续生效。没有任何所属组时不显示安全告警，仍可查看本人设备提醒。
- 列表、筛选选项、总数/待办数量、详情、操作记录、首页摘要及通知继续使用统一的事件权限规则。作业组列表及其查询接口未修改。
- 安卓消息页普通账号页头改为“所属各组人员告警 · 本人设备提醒”，数量沿用“我的待办”；管理员现有文案和布局保留。
- 修改范围：`EventAccessService.java`、`InspectionEventScopeTest.java`、`events_page.dart`、`wear_event_scope_ui_test.dart` 及本记录。验证完成：后端独立 Docker 源码副本编译及 32 项专项测试通过（范围 15、流程 6、状态机 5、筛选 6）；安卓相关回归 22 项通过，页面及对应测试静态分析无问题。覆盖无任务/其他任务的本组人员告警、绑定本组任务的外组人员告警、多个组和已结束组的人员并集、重复成员、移除成员、详情与列表/数量及设备提醒隐私。证据：`C:/melhat-runtime/event-person-scope-backend-tests-20260922.log`、`C:/melhat-runtime/event-person-scope-android-tests-20260922.log`、`C:/melhat-runtime/event-person-scope-android-analyze-20260922.log`。
- 本轮未部署、未安装 APK、未修改业务数据库或 5188 平台、未提交推送。集成部署需同时包含主后端人员权限规则与安卓文案；不能仅更新安卓后宣称服务端规则已生效。

## qa_chen 运行排查与进行中作业组限制（2026-09-22）

- 用户报告重新构建部署后仍看到外组消息，确认账号为陈建国 `qa_chen`；随后明确补充：组状态也要参与授权，只显示正常进行的业务，完成或取消的组不能提供权限。本节替代上节“不限定组状态”。
- 实际接口复现：`qa_chen` 是非管理员，userId=114、本人 personId=10；进行中所属组成员为人员 10～16。旧运行接口在 `status=all` 下返回 123 条，其中可定位截图低电量事件 165/166，人员为 3“演示人员01”，既非本人也不在授权成员内。
- 部署根因：Docker 内旧 JAR 时间为 10:52，内嵌系统模块缺少 `EventAccessService`，`EventQueryService` 也没有事件权限依赖。`docker/backend/start.sh` 仅在 JAR 不存在时编译，因此单纯重启会复用旧包。APK 更新并不能更新后端权限。
- 代码限制：普通账号只从 `status='in_progress'` 的所属组汇总成员，排除 draft/ready/paused/ended 和任何未知、取消、完成状态。巡检组有巡检项且全部已有完成记录时，即使主任务状态尚未切换 ended，也不提供权限。仍在其他进行中组的人员继续可见；组状态不改变管理员的全站安全告警权限、本人设备提醒和原事件状态筛选。
- 回归先红后绿：不限制组状态时 3 个用例失败；仅限制主任务状态后，巡检项全部完成的用例仍失败，已补齐此边界。安卓页头改为“进行中组内告警 · 本人设备提醒”，9 项组件回归及静态分析通过。
- 已备份运行旧包至 `C:/melhat-runtime/event-active-scope-release-20260922/fdc-admin-before.jar`。包含巡检完成边界的最终包编译和 34 项专项测试通过（范围 17、流程 6、状态机 5、筛选 6），已替换 `melhat-backend:/workspace/ruoyi-admin/target/fdc-admin.jar` 并重启主后端。最终实测 `qa_chen` 的范围外事件为 0、全部可见事件 50、待办 22；事件 165/166/167 的普通账号详情请求均返回 403，管理员查询安全事件 167 返回 200。已运行同一复现脚本由失败转为通过，无需重装 APK 即可获得后端过滤结果。
- 证据目录：`C:/melhat-runtime/event-active-scope-release-20260922/`（build.log、build-final.log、api-verification.log、source-hashes.json、旧包备份及新包）。只读复现脚本 `C:/melhat-runtime/debug-event-person-scope.py`，凭据在本地读取且不输出；旧包检查 `C:/melhat-runtime/debug-event-running-package.log`。没有数据库迁移、APK 安装或 5188 平台修改。
- 部署基线：HEAD `063bfdf042b58b6de98d823912685872d2000941` 加当前工作区主后端源码，在独立 Docker 源码副本执行 Maven package，并运行上述测试。最终产物 `fdc-admin-final.jar`，哈希见 `final-package-sha256.json`；最终业务验证见 `api-verification-final.log`。源目录及其他任务未提交改动均保留，没有提交推送。安卓页头文案仅源码更新，本次没有重新安装 APK。


## 事件分级、照片处置与快速筛选（2026-09-22，14:45 更新）

本节记录本轮最终规则，替代此前“全部安全异常上报后由管理员复核”和按 low/high 判断设备提醒的规则。

- 按实际来源设备快照 `deviceType` 与已有代码分级。SOS 为紧急；设备类型匹配的 battery/low_battery 为警告；其余现有风险记录为异常。没有凭模拟场景目录新增检测能力，也没有推断高处场景或阈值。保留原“跌落”设备告警，不称“人员跌倒”、不据此自动升级紧急。当前已有命名记录来自 simulator，仍明确显示“演示”。
- 警告：仅本人可见，包括管理员也不可读取他人警告；详情点击“收到”通过版本校验直接关闭。异常：进行中所属组成员和管理员可见，现场拍摄 1–4 张照片并填写说明，提交成功直接关闭。紧急：同样可见，现场上报后保留待审批状态；管理员审批通过后关闭，上报与审批不能为同一账号。普通账号仍受进行中组、人员、厂站三重范围限制。
- 消息页新增紧急/异常/警告单选快速筛选，可再点取消；服务端筛选分页与总数保持一致。警告绿色、异常橙色，无等级背景色；紧急浅红卡片、加粗红色边条、红底等级标签与优先处理提示。保留原 Hero、Logo、底部导航、分页和原筛选能力。
- 详情按等级区分：警告为本人确认；异常为现场说明、真实相机拍摄和上传；紧急增加“现场上报—管理员审批—结束”进度与审批入口。失败/冲突保留草稿。照片通过有权限校验的接口回看，不公开文件路径，不再使用示例附件。服务端验证实际 JPEG/PNG 内容、数量/大小/像素上限；失败事务同时回滚媒体行及文件。原 JSON handle 不能绕过照片要求。
- 数据库已执行 V018/V019：severity 扩为 VARCHAR(16)，增加设备类别快照与媒体表。备份后删除 153 条名称和代码均空、或名称为占位文字的无效事件及其事件从表；独立通信记录保留并解除无效事件关联。命名有效的旧异常若处于待复核，则记录迁移动作并转为可补拍状态，不伪造照片或自动判作已处理。新入库拒绝无名称/无代码事件；内部围栏事件补齐现有规则对应名称和编码。
- 部署：主后端 `melhat-backend` 已替换并重启，健康检查通过；APK 使用当前工作区 debug/main.dart，API_BASE_URL=http://10.143.40.195:18084，CALL_LAB_ENABLED=true，安装目标雷电 emulator-5554。未提交推送，未修改或部署 5188 平台。
- 验证：后端 47 项专项测试通过；安卓 35 项回归通过，7 个本轮业务文件静态分析无问题。实际 API 验证警告隐私/确认、异常照片提交即关闭、紧急待审批/普通账号不可审批/管理员结束、三等级筛选及无名称入库拦截；陈建国 qa_chen 范围外记录为 0。雷电检查顶部筛选、列表紧急区别、警告详情确认、异常表单、返回列表和底部导航。3 条临时演示事件及其测试媒体已清除，无效占位事件余量为 0。
- 环境限制：雷电未安装可处理 android.media.action.IMAGE_CAPTURE 的相机应用（resolve-activity 返回 No activity found）。已验证拍照入口和权限请求，但未把模拟器实拍称作成功；手机真实拍摄仍需客户设备验收。上传与回看链路已用明确的测试图片经真实 HTTP 验证。
- 证据目录 `C:/melhat-runtime/event-levels-release-20260922/`：build-final.log、tests-pass.log、analyze-pass.log、workflow-live.json、scope-live.json、list-grades.png、detail-warning.png、events-before.sql、fdc-admin-before.jar。list-grades.png 使用已清理的明确演示事件展示紧急样式。照片目录为 Docker 既有持久卷下 `/data/event-evidence/`。备份和部署产物留在本地，未纳入 Git。

## 详情卡片精简与可选照片、视频（2026-09-22，16:37 后续需求）

本节以用户最新截图和要求为准，替代上文异常、紧急事件必须填写说明并拍摄照片的限制。用户明确要求本轮不打包部署。

- 移除详情中的“当前状态”整张卡片以及紧急事件的“紧急事件处理进度”卡片。保留详情顶部状态、上报表单和事件记录；警告“收到”、管理员“审批通过并结束”保留为独立操作入口。
- 异常原因和现场附件全部选填，安卓及后端均允许空说明、无附件提交。异常提交即关闭；紧急提交进入待管理员审批，仍需不同账号完成审批，未绕过权限、版本和并发校验。成功提交空内容也能正确识别已上报，未提交的空草稿不会被误判。
- 沿用附件卡片、添加按钮和缩略图布局，同一入口提供现场拍照、现场录像、相册照片/视频多选。支持混合添加最多 6 个附件，照片每张 10MB、视频每个 50MB、合计 100MB；可移除、本地预览视频，提交后通过原鉴权接口回看。继续使用已有草稿字段和媒体表，无数据库结构变更。
- 上传保留真实文件名并采用文件流；后端识别实际媒体内容，沿用照片解码校验和事务失败清理。Docker 后端源配置的 multipart 限额调整为单文件 50MB、请求 102MB，以容纳附件与表单开销。
- 验证：安卓相关 29 项测试通过，覆盖空说明/无附件、混合多媒体、失败/冲突保留草稿、移除卡片和不同等级流程；后端 39 项测试通过，覆盖省略 comment/files 的 multipart 请求、空提交、混合媒体、文件限制、授权和审批。相关 Dart 静态分析无问题，限定改动范围的 `git diff --check` 通过。
- 证据：`C:/melhat-runtime/event-optional-media-20260922/flutter-tests.log`、`backend-tests.log`、`analyze-final.log`。后端仅在独立 Docker 测试容器的源码副本运行 Maven test，没有生成部署包、替换运行服务或修改业务数据。未打包 APK、未安装、未部署、未重启，未修改 5188 平台；真实设备拍摄及视频播放留待后续部署后验收。后续生效需同步更新安卓、主后端及其上传限额配置。

### 选填页面仍提示必拍照片的运行版本核验（2026-09-22，17:27）

- 用户截图页面已显示可选照片/视频，但提交返回“请现场拍摄1至4张照片”。只读核验 Docker `melhat-backend`：运行 JAR 修改时间仍为 15:22，SHA256 `62a52001676ffdfaade0a64f2e60f8f8299d59f98d4b427f520b95ec9b2b1cc2`，与此前巡检修复部署包一致。
- 从运行容器复制 JAR 后检查实际 class：`EventEvidenceService.class` 仍含截图完全相同的提示，`EventCommandService.class` 仍引用 `requirePhotos`。工作区源码已移除两者，空附件/空说明的 39 项后端专项测试记录仍为通过。根因为新版安卓页面与尚未更新的主后端校验不一致，不是选填 UI 文案失效。
- `docker/backend/start.sh` 仅在 JAR 不存在时构建；重启不会替换既有旧包。运行环境生效需要重新构建并部署主后端及上传限额配置。
- 本轮遵守此前“不打包部署”的要求，仅做源码/运行包只读检查，未提交任何真实事件、未改变业务数据或运行服务。证据 `C:/melhat-runtime/event-optional-runtime-check-20260922/result.json` 与该目录的运行包副本。

## 主后端重新构建部署及上传限额生效（2026-09-22，17:35 后部署复查）

- 依据用户本轮明确要求“重新构建并部署主后端docker，同时更新附件上传限额配置”执行。基线为 HEAD `063bfdf` 加当前工作区后端源码，源码复制到独立 Docker 构建目录，构建前后来源哈希一致，保留所有既有未提交修改。
- Maven `package` 成功，55 项通讯读取、事件附件/流程/范围、巡检访问及成员进度测试通过。已备份旧 JAR，将新 JAR 替换至 `melhat-backend:/workspace/ruoyi-admin/target/fdc-admin.jar`，仅重启主后端；容器状态 `running healthy`，实际运行包 SHA256 为 `3fdd0f5610ecc08fb884e0ed8674acc08696d9a13dde94092c012b240b143129`。检查产物已移除旧“请现场拍摄1至4张照片”校验，包含视频附件实现。
- 现有 `docker/backend/application.yml` 的 multipart 配置随本次重启生效：单文件 50MB、单请求 102MB。业务限制保持照片每张 10MB、视频每个 50MB、最多 6 个附件、合计 100MB；说明及附件可选。
- 部署后接口检查发现当前完整源码还包含手动 SOS 的 V020 依赖，数据库缺少 `reporter_user_id`。先完整备份 `wear_safety_event`，再执行已核对的 `V020__manual_sos.sql`，仅新增可空 BIGINT 字段，不改历史事件内容。字段核验通过，事件接口恢复。此次迁移实际已执行，更新前文“未执行”的部署状态。
- 真实 HTTP 验证：登录、个人信息、事件列表、消息数量、我的作业均返回 200；不存在事件返回预期 404。针对不存在事件的 multipart 请求验证上传解析：空附件、50MiB 单文件、98MiB 请求通过解析并返回资源不存在；50MiB+1 字节单文件、超过 102MiB 的请求被上传限制拒绝。超限沿用现有响应封装（HTTP 200 / 业务码 500），未新增错误处理行为。此验证没有提交真实事件，也不冒充真实照片/视频保存成功。
- 证据与回退备份：`C:/melhat-runtime/backend-release-20260922-173033/`，含 `build.log`、`source-hashes.json`、`fdc-admin-before.jar`、`events-before-v020.sql`、`artifact-sha256.json`、`runtime-upload-config.txt`、`migration-check.txt`、`upload-checks.json`、`api-smoke.json`。5188 独立平台的容器 ID 和启动时间前后相同，源码及运行环境未修改。本轮未重新安装 APK、未提交或推送；真实设备拍摄与视频回看由用户验收。
- 补充检查：同一源码快照的 `ManualSosTest` 5 项全部通过，合计本轮 60 项专项测试通过；日志 `manual-sos-tests-final.log`。最终再次核验运行 JAR 哈希一致、容器健康。
