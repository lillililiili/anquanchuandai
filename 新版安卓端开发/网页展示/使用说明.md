# 当前安卓样式 · 静态网页展示

2026-09-21，根据当前安卓工作区制作。双击 **打开安卓演示.cmd**，即可打开 http://127.0.0.1:18766/ 。无需运行后端、数据库、Flutter 或模拟器。结束时双击 **停止安卓演示.cmd**。不要直接双击 site/index.html，Flutter 资源需要由附带的本地静态服务器提供。

手机内沿用当前安卓的实际页面组件、图片、图标、配色、按钮、底部导航和弹窗。鼠标滚轮或触控拖动可连续查看下方内容，点击按钮可进入子页面。桌面左侧目录提供页面入口；较窄窗口自动隐藏目录，保留手机展示。

默认提供两个预设演示账号，左侧支持一键无缝切换或在登录页输入体验：
- **管理员 (admin)**：系统管理员（权限 `*:*:*`），具备全厂调度、全局态势、作业研判、值班交接以及系统设置完整管理权限；
- **巡检员 (qa_chen)**：一线巡检员/班组长（陈建国，工号 `P-001`），自动关联专属智能安全帽 `RL-H001` 与智能定位腰带 `BL-DEMO-001`，展示作业执行、装备状态、轨迹回放与现场通讯。

在登录页输入账号 `admin` 或 `qa_chen`（密码任意）即可按对应身份登录。所有按钮控件（登录、快捷呼叫、SOS、任务流转、状态处置等）均直接通过安卓原生 Flutter 组件代码渲染，确保样式、尺寸、圆角与交互反馈与安卓端 100% 一致。厂站切换、筛选、搜索、分页和操作均在内存中实时响应，刷新即可重置。

## 保留的数据

只读导出现有数据库内容：2 个厂站、50 名人员、68 台设备、109 条领用记录、30 项作业、172 个事件、6 个围栏、5 条交接记录、40 条通话历史、24,048 条历史采样。实际各厂站页面数量按所属关系和筛选条件计算，不将所有数据混放在同一厂站。人员、设备、作业、事件之间的关联保留。

历史采样使用原发生日期，查看轨迹时请选择有数据的历史日期（主要为 2026-08-20 至 2026-09-18），不伪造为今天的实时定位。保留安卓内置的“示例”轨迹入口。设备在线状态为快照，运行时间不代表实时设备状态。地图只保留已缓存的事件底图；其他底图依赖外部地图服务，离线时显示安卓原有坐标或加载失败提示。

## 页面关系与验收

- [页面关系](页面关系.md)：主页面、子页面和弹窗的进入方式。
- `同步验收/20260921/页面截图/`：四个主页面顶部和连续下部截图，保留重叠区域便于检查衔接。
- `同步验收/20260921/主项目只读快照.json`：读取主项目时的文件哈希。
- `同步验收/20260921/验收结果.json`：原项目未改动、共享 UI 一致性、数据与构建核验结果。
- `文件校验清单.json`：本次发布文件的 SHA-256。

## 目录与重新构建

`site/` 为可直接发布的静态文件；`展示源码/` 为独立复制的安卓源码；`历史备份/` 保存本次替换前的展示站点与压缩包。

完整项目可运行 `重新构建.ps1`。它只在本目录的 `展示源码/` 内构建，使用已锁定依赖和 `CALL_LAB_ENABLED=true`，不进入或修改安卓主项目。构建需要 Flutter 3.41.3 及本地依赖缓存。可直接运行压缩包不含源码及 Flutter 环境。

本次与安卓共享的页面源码保持一致，展示差异限定在独立入口、本地数据适配、网页外框及测试文件。所有业务请求均由本地适配器接管，无后端网络回退。中文字体、图片和 CanvasKit 均随包提供。
