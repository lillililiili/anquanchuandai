# 界面业务化文案验证

日期：2026-09-20。

## 后续登录调整（同日）

当前登录页已统一为正式账号密码表单，替代下述早期身份选择设计。登录区域不再显示运行模式说明、身份下拉框或重置入口。后端未接入时由独立本地提供器校验固定账号密码；正式构建仍调用后端认证，不包含本地凭据。账号见 README。

验证：165 项单元测试及 lint 通过；浏览器验证必填提示、错误密码拒绝、正确密码回车登录后进入总览。所有角色的账号映射和无密码拒绝由传输层测试覆盖。此次界面调整不代表后端认证或生产安全验收完成。

旧 `scripts/browser` 中依赖身份选择或场景控制面板的历史验收片段不再适用于当前入口，未将其计入本次验证；当前验证使用真实页面交互及传输层测试。

## 修改范围

- 新前台 src 内中文提示、种子显示名称、业务编号、操作结果及导出名称；保留内部契约、权限、路由标识与请求隔离。
- 移除公共布局中的开发场景控制入口；使用“服务未接入”说明当前连接状态。
- 本地入口使用“进入工作空间 / 进入系统”，没有新增假密码认证。
- 视频资产与抓拍水印使用“本地视频 · 非现场画面”；未将合成画面宣称为真实直播。
- 同步已有测试中对应的文案断言，新增 ui-copy.test.mjs 防止旧字眼回归。
- 未修改后端、数据库、原 PC 或安卓端。未升级工程依赖。

## 验证

- 单元测试 164 项通过；lint 通过。
- production、staging、demo、mock 四种构建通过。
- 浏览器检查登录、安全总览、人员列表及摘要、作业监护、事件列表、调度协同、现场资料及视频墙；这些页面可见正文未出现旧中文标签或 MOCK / DEMO 标记。
- 视频手动播放成功，截图确认水印更新。
- 未重新逐一操作全部写入弹窗和所有异常分支；这部分由现有逻辑测试及源码中文扫描覆盖，不能称为完整端到端验收。

## 保留边界

当前本地数据仍是预置内容，刷新恢复初始状态。登录身份选择不是服务器认证，通信不会连接设备，文件与业务操作未持久化至服务器。正式认证、生产业务数据、真实设备、服务端权限与生产安全验收仍待接入。历史实施文档及内部技术命名保留，未将历史验证结论改写为生产验收。
