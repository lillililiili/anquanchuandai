# S4 验证记录

日期：2026-09-17。结论：告警与现场核验“页面与接口基础验收”通过；不代表真实业务联调、真机验收或核验闭环完成。

## 隔离测试与构建

| 项目 | 结果 |
| --- | --- |
| 前端逻辑测试 | 74 项通过：S1 17、S2 16、S3 21、S4 20 |
| PortalEventTest | 32 项通过，无失败/跳过 |
| 后端回归 | PortalServiceTest 24、PortalS2Test 24、PortalVideoTest 19、RealDeviceModeTest 6、PlatformDeviceSyncTest 3 |
| 后端本轮合计 | 108 项通过，测试来源隔离，无真实数据库/设备连接 |
| lint | 通过，无警告 |
| build:stage、build:prod | 通过；最终 dist 为 production |
| S4 浏览器开发服务 | 46 项通过 |
| S4 浏览器生产预览 | 46 项通过，未发现资源路径错误 |
| S1–S3 浏览器回归 | 人员 38、空间资料 36、视频 35 项通过 |
| 登录及异常回归 | auth.js 32、edge.js 11 项通过 |

浏览器 S4 覆盖：未登录拦截、初始不选择、键盘选择、独立统计、旧处理状态与核验分离、未知历史归属、只读表单/禁用操作、历史和当前装备分离、时间线与核验记录独立分页、返回恢复、时区转换、筛选分页清选择、请求竞态、厂站切换、详情/统计失败不影响列表、记录无权限、无记录、不可见事件、空数据/未接入/来源故障、会话失效与目标保留。

后端覆盖五个 GET、登录/模块权限/核验额外权限、厂站和对象范围、非法筛选、分页、半开时间区间、不同时间小数精度排序、来源异常、人员归属证据、资料权限、敏感字段不泄露、跨来源身份、统计一致性、草稿不等于提交、回传不等于本地完成。

测试中遇到并已修复：Java Record 导入歧义；浏览器脚本误用组件不存在的英文分页名称；时间字符串排序在不同小数精度下可能颠倒。分页补充了可读的上一页/下一页文字，旧 auth.js 的告警占位断言同步为未接入页面。修复后相关测试已重跑。

## 视觉与请求边界

已在 1672×941、1440×900 检查 F13/F12 及长文本场景。与 PDF 保持深蓝、多分区、左侧事实/右侧摘要或核验布局；正文无横向溢出，长内容允许纵向滚动。分页具备文字名称，选择按钮保留键盘焦点及选中语义，状态不仅依靠颜色。

截图保存在忽略目录 `output/playwright/s4-F13-1672.png`、`s4-F13-1440.png`、`s4-F12-1672.png`、`s4-F12-1440.png`、`s4-long-1440.png`。截图含测试专用合成事件，不是正式业务数据。

`scripts/browser/events.js` 只在专用浏览器会话拦截网络，结束时解除拦截、清除测试 Token 并返回真实登录页。未加入应用内演示开关、模拟登录或生产数据回退。业务请求限定 GET；认证流程使用测试拦截的登录接口。检查未发现外部底图、真实影像、存储、RTC、设备命令或业务写请求。

## 真实环境只读探测

- `GET http://127.0.0.1:18084/captchaImage`：HTTP 200，业务 code=200，captchaEnabled=false。
- 匿名 `GET /api/portal/v1/events?siteId=probe`：HTTP 200，业务 code=401。
- 401 仅说明现有登录过滤生效，不能证明新 Controller 已部署、真实权限链或数据已联调。
- 本轮未部署/重启后端，未创建账号、分配权限、修改数据库、生成迁移或修改设备配置；没有升级依赖。既有未提交修改保留。

## 未验证与后续门槛

真实账号、厂站/对象授权来源、旧事件到厂站映射、真实事件类型与阶段、核验历史及人员归属证据均未接入验收。默认 PortalEventSource 明确未接入，不读取旧报警表或设备平台。全局安全过滤链的真实部署验证仍需授权账号及受控环境；本轮权限测试为隔离 MockMvc/服务验证。

原图访问、播放、下载、抓拍、认领、转交、保存、提交、上传和回传均关闭。后续需单独确认持久化、并发、幂等、审计、受控存储与外部状态映射；不得将隔离测试通过解读为这些能力可用。安全带/手表没有协议，未生成相关告警或传感数据。

详见 [S4 实施与契约](../../docs/智能穿戴设备平台/07-S4告警核验实施记录.md)。
