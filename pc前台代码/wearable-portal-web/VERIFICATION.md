# 前台基础框架验收记录

验证日期：2026-09-16（America/New_York；UTC 2026-09-17）。范围仅为新工程基础框架，不代表完整业务或生产安全验收。

## 工程与运行

| 项目 | 结果 |
| --- | --- |
| npm 独立安装与锁文件 | 通过；`npm ls --depth=0` 核心版本与确认计划一致，Vue / compiler-sfc 均为 3.5.12 |
| `npm run lint` | 通过，无错误、无警告；包含保留的浏览器验收片段 |
| `npm run build:prod` | 通过，最终构建包含 favicon、背景和全部懒加载页面 |
| `npm run build:stage` | 通过 |
| 新旧 PC 分别运行 | 通过；旧 PC 5175 登录页可加载，新 PC 5176 独立运行 |
| 严格端口检查 | 通过；已有前台运行时第二次启动直接返回 `Port 5176 is already in use`，未换端口 |
| 生产预览 5177 | 通过；九菜单、定位标签和刷新正常，无页面 JS 运行异常、无静态资源 404 |

生产预览认证使用浏览器拦截 `/prod-api` 的模拟响应。正式网关与生产后端尚未联调；预览服务本身不提供生产 API 代理。

## 认证与导航

真实后端 `http://127.0.0.1:18084/captchaImage` 返回 HTTP 200、`code: 200`、`captchaEnabled: false`；通过开发代理显示为无需验证码，未登录深链接正确跳转到登录页并保留目标。

浏览器模拟响应验收：认证与页面流程 32 项、异常边界 11 项、生产预览与双工程运行 5 项均通过，合计 48 项（不含上述真实验证码配置及严格端口检查）。

- 账号/密码必填、密码显隐、提交禁用和 loading。
- 验证码开启、刷新、提交 `code/uuid`、登录失败提示及自动刷新；失败后清空旧验证码。
- 模拟成功后读取用户信息，Bearer 请求头和独立 sessionStorage 键正确，未写入旧后台 `Admin-Token`。
- 登录恢复 `location?tab=tracks`；刷新保留当前页面；空角色数组不引起反复初始化。
- 九个菜单均显示真实建设状态，无模拟业务数量、地图点位或在线状态。
- HTTP 401、业务 401 并发时只提示一次并清理会话；刷新遇失效会话仍保留目标。
- 登录后 `/getInfo` 不完整时回滚凭证；后端不可达不会创建假会话。
- 拒绝外部、双斜线、反斜线、登录循环、未知页面等返回目标。
- 退出调用后端并清理会话；退出请求失败仍清除本机 Token 并提示服务端状态未确认。
- 厂站按钮禁用；开发管理中心地址正确；生产未配置后台地址时禁用入口。
- 401、404页面可访问，定位三个标签及刷新恢复正常。
- 角色和权限数组已保存，权限工具正/反向判断通过。

真实账号登录、真实角色权限及服务端退出尚未验证：当前没有可用于联调的登录账号。用户临时要求创建账号后，调用现有注册入口返回“当前系统没有开启注册功能”；未创建用户、未修改注册开关、未修改数据库。用户随后明确暂缓账号创建。测试中的 fixture 账号只能用于浏览器模拟，不是可交付登录账号。

## 视觉检查

按前端设计及 UI/UX 指引检查了统一深蓝主题、页面层级、表单可见标签、图标按钮可访问名称、焦点样式和建设中空状态。以 PDF 为方向，不使用指引推荐的其他配色替换设计稿。

1672×941、1440×900 均完成截图及布局尺寸检查：

- 登录页、开启验证码并显示错误的表单无溢出。
- 顶栏、展开侧栏、折叠侧栏、正文面板无横向溢出。
- 生产定位页面显示三个标签，无虚构地图内容。

截图保存在 `output/playwright`（Git 忽略）：`login-real-*`、`login-captcha-*`、`overview-*`、`collapsed-*`、`preview-location-1440.png`。

当前视觉差异已明确保留：PDF 航拍原图和独立品牌图标未提供可复用源素材，登录背景暂用旧工程工业图片，品牌显示 ROLLING 文字。取得正式素材后替换，不应宣称当前版本已达到 PDF 像素级还原。

## 安全与交付边界

- `npm audit --omit=dev --registry=https://registry.npmjs.org` 检出两个依赖告警：Axios high、Element Plus moderate。部分 Axios 告警涉及 Node 适配器，但不能据此声称前端版本无风险；上线前需逐项评估并安排兼容升级。
- 当前 npm 镜像源不支持 audit，审计时仅对该命令切换官方 registry，未修改用户全局 npm 配置。
- 未执行自动依赖升级；未建立设备控制、RTC、WebSocket 或地图连接；未调用业务数据写入接口。
- 旧 PC、安卓、后端代码及数据库结构未修改；工作区中原有未提交修改保持原状。
- 浏览器模拟拦截已解除，专用测试页已回到真实登录界面，不在应用中保留模拟登录开关。

复测片段位于 `scripts/browser`，执行顺序和命令见 README。后续联调应补充真实账号登录、后端角色/厂站数据范围、HTTPS 网关与生产会话安全验证。
