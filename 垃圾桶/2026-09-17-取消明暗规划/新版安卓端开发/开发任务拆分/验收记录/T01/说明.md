# T01 外观设置开发与验收记录

日期：2026-09-17。用户授权范围：仅T01，完成后覆盖安装雷电。已完成开发、构建和雷电覆盖安装；开发自验通过，任务状态为待验收，供用户查看。

## 本次可见改动

- 在“我的”现有全部内容后补“设置”行；原接警、装备、交接、切换厂站、退出登录及版本文字顺序保留。
- 点击设置打开局部外观面板，浅色／深色二选一，点击即时更新面板颜色与选中标识，关闭返回原页面和滚动位置。
- 使用已有shared_preferences存储安装级偏好；初次浅色，冷启动读取。保存失败原位提示，重选可重试。
- T01仅适配这个面板。四栏、其他页面、现有背景保持原状；不代表全App深色开发完成，T02以后未启动。

## 文件范围

相对安卓工程 `android代码/melhat_android-main`：

| 文件 | 修改内容 |
| --- | --- |
| lib/wear/app.dart | 创建和释放单一外观控制器；在现有路由外提供外观状态，保留原路由／ThemeMode.light／会话 |
| lib/wear/mine_page.dart | 只追加设置行，打开面板 |
| lib/wear/preview_appearance.dart | 新增外观状态、串行偏好写入、读取竞争保护、局部面板与失败反馈 |
| test/wear_preview_appearance_test.dart | 新增6项行为验证；包含360dp普通／1.5倍字体，偏好失败及并发情形 |

未修改pubspec、依赖锁文件、图片、客户Word、业务接口和Docker配置。三个生产文件符合T01限额。

## 自动化结果

- 开发前原wear_app_test.dart：5项通过。
- 新UI测试在未实现时失败：找不到“设置”入口，确认能捕获缺失功能。
- 实现后：wear_preview_appearance_test.dart与wear_app_test.dart共11项通过。
- flutter analyze --no-pub检查上述3个生产文件及新增测试：No issues found。
- git diff --check：无空白错误；Git仅提示工作区LF／CRLF转换。
- 覆盖快速点击最终值持久化、迟到读取不覆盖用户选择、读取／保存失败、重试、释放后异步完成、冷启动、原路由对象／账号／厂站／通话活动标志保持。
- 模拟器UI验证不使用mock；自动化测试使用工程既有测试身份与HTTP替身，不修改生产API。

## 原图与边界

参考浅01／深01的设置入口语义、白底蓝色及深蓝卡片白字。原图没有展开的外观面板，本次仅增加必要的浅深选项，不声称新增面板存在逐像素原图。设置放原内容末尾是T01明确的局部增补，未重排整个“我的”。

启动检查曾发现旧App网络错误；宿主机Docker健康，但ADB临时映射为空。只运行既有connect-ldplayer.ps1恢复连接，健康返回UP；使用原“重新验证已有会话”恢复当前登录，无改后端或账号。旧登录头部已有溢出提示留作基线，不在T01顺带改版。

## 回退

本目录rollback保存本任务开始前两个生产文件、任务文档和从雷电直接拉取的before.apk。来源相对路径与哈希见文件清单.json。仅恢复这两个生产文件，将本次新增Dart和测试按原相对路径移入单任务垃圾桶；不回退其他历史修改。需要恢复安装时使用before.apk覆盖安装并运行原连接脚本，不卸载清数据。

## 雷电实测结果

- 构建assembleDebug成功；install -r返回Success。应用冷启动Status: ok、LaunchState: COLD。
- 入口：我的→下滑至原内容末尾→设置。浅色面板首次选中浅色，点深色立即变为深蓝底白字，选中标识同步。
- 关闭面板仍回到我的原滚动区域；再次打开保留深色。强制停止App后重启，默认现场与四栏保留，再进入设置仍为深色。
- 冷启动后的我的页显示原账号admin、原厂站演示厂站A；本次没有切换账号或厂站，没有执行业务提交。
- 深色→浅色再次切换通过；最后保留深色面板供查看。截图均来自雷电实际安装包。
- 本地APK与雷电已安装base.apk的SHA256一致：`f3f7d32e623aac947e650b1c9d2aa34e5cc0a281acf631dda59e0756ca7fff21`。
- APK：`android代码/melhat_android-main/build/app/outputs/flutter-apk/app-debug.apk`。构建参数android-x64／debug／API_BASE_URL=http://127.0.0.1:18084。
- 后端健康返回UP；没有为本任务更改任何业务API。
- 将两个T01修改文件替换为回退备份、排除两个新增文件后，405个受保护文件的综合哈希与开发前完全一致，确认没有夹带其他安卓／图片／客户方案修改。

## 截图与留档

[入口](after-entry.png) · [浅色面板](after-light.png) · [深色面板](after-dark.png) · [返回我的](after-return.png) · [重新打开](after-reopen-dark.png) · [冷启动仍深色](cold-start-dark.png) · [再次切浅](after-switch-back-light.png)。

[修改前我的顶部](before-mine-top.png)和[修改后顶部](after-mine-top.png)用于核对原内容；[修改前末尾](before-mine-bottom.png)用于核对新增入口。before-light.png为最初连接尚未恢复的旧登录画面，仅保留问题追溯，不冒充我的页对照图。

完成代码快照在completed目录，原始工作区文件与旧安装包在rollback目录；都按项目根相对路径保留。没有Git提交或远程推送。本轮不启动T02。
