# T44 围栏详情关联内容｜UI微任务实施计划

> **执行者要求：** 后续获准开发时使用 superpowers:executing-plans 串行实施，只执行本任务。本轮仅写计划，所有步骤均未实施。

**目标：** 让安卓端“FencePage”完成本任务所述可见微调，验收覆盖：无人员、未知位置、关联事件和返回。

**方式：** 在回退版本上局部调整，保留现有业务控制器、四栏及API，不重写整页。

**技术栈：** Flutter/Dart、现有GoRouter与业务控制器；雷电验证UI，Docker后端延用。

**依据：** [10开发指南](../10-安卓开发指南-预览图复刻与微调.md)、[11逐页说明](../11-安卓逐页开发说明与跳转保留表.md)、[12强限制](../12-安卓开发强限制与验收清单.md)、[客户方案](../智能穿戴管理平台-整体设计方案.docx)。

## 强限制

- 保留“现场／通讯／消息／我的”四栏、现场默认首页，不重新分配页面职责。
- 浅深分别按原图；原模块顺序、主体构图和按钮区域保留，不复用已撤销实现。
- 不重画三维厂区、不改围栏后台规则。
- 本轮只做文档；下列步骤是后续计划，不生成图片、不修改代码、不部署。

## 范围与前置

- **状态：** 未开始；没有本任务生成的素材或APK。
- **依赖：** [T43](T43-围栏列表.md)
- **原图：** 无独立原图；以当前回退版布局为基线，仅继承父目录11指定的视觉语言，禁止新设计页面。
- **定位：** FencePage。
- **候选修改文件（相对安卓工程根目录）：**
  - `lib/wear/queries/fences.dart`
- **衔接：** 原人员与type=geofence链接兼容。 契约见[03主题与素材衔接说明](03-主题与素材衔接说明.md)。标“新增”的文件是施工候选，不是当前已存在的实现。
- **幅度：** 一处连续区域或一个原流程的小闭环；一般不超过三个生产源文件，测试与资源清单另计。超出先拆成T44a／T44b，再排期，不扩大原任务。

## 修改步骤与目的

这些步骤共同组成一个可验收小任务。读图、基线截图、配置与测试都附在本任务，不单独冒充UI成果。若某处已经符合要求，保留证据，不为了完成任务制造差异。

- [ ] **步骤1：只适配原分组颜色与边界，区域信息结构保持。**

  **目的与可见结果：** 把上述原区域的具体控件贴近对应参考图；修改前后截图必须能指出本步骤影响的位置。

- [ ] **步骤2：缺位置或适用范围原位未知，不猜层或扩区域。**

  **目的与可见结果：** 让上述原区域的主题、数据或交互反馈准确；只处理已写明的小项，不迁移其他功能。

- [ ] **步骤3：原适用人员和关联事件继续正确对象与type筛选，返回该围栏。**

  **目的与可见结果：** 让用户在这个原入口完成正确动作、保持正确对象和返回关系；不得借此重做导航或业务控制。


## 验收步骤

- [ ] **步骤4：原图对照。** 保存本区域修改前、浅色修改后、深色修改后截图。涉及图片生成的任务必须把图片引用到本区域并在雷电看到切换，不能只交PNG。
- [ ] **步骤5：具体场景。** 在安卓UI中验证：无人员、未知位置、关联事件和返回。有输入／选择时切主题并返回；纯展示区检查同一对象、滚动和状态保持。
- [ ] **步骤6：原入口闭环。** 从本页面原入口进入“FencePage”，操作原控件到实际目标或局部面板，再返回来源；核对人、设备、事件或任务ID。进入页面不得自动拨号、播报或业务提交。
- [ ] **步骤7：相邻回归。** 检查该区域上下相邻内容及同组件的其他调用，未列入范围的布局和功能不得改变。无接口时本地交互与明确不可用反馈仍应完整，不删模块。
- [ ] **步骤8：必要自动化。** 纯样式以截图为主，不写镜像实现测试；若涉及状态、导航、请求或修复缺陷，先以本任务具体场景复现问题，再补最小回归并确认通过。

测试定位：`test/wear_queries_test.dart`。新增外观、附件、多选状态测试随对应任务创建，不在本轮生成。建议命令仅在后续实施、安卓工程目录执行：

```powershell
& 'C:/melhat-runtime/flutter/bin/flutter.bat' test --no-pub test/wear_queries_test.dart
```

环境、构建与雷电步骤见[02逐任务验收与回退规则](02-逐任务验收与回退规则.md)。自动化通过不能代替原图视觉通过。

## 交付与回退

- [ ] **步骤9：单任务交付。** 在雷电展示本区域，交付浅深截图、原入口到目标及返回结果、实际改动文件与原接口结果。区分本地UI完成、远程能力开通及真实硬件验证。
- [ ] **步骤10：独立回退点。** 保留本任务修改前文件／APK以及新增资源相对路径；只包含本任务变更。失败只修或恢复本任务，不撤销此前已验收任务。
- [ ] **步骤11：状态更新。** 将任务表改为待验收或已验收，并记录证据。只有本任务通过后才进入依赖它的任务；用户只指定本任务时，到此停止。

可直接下达的指令：

> 只执行T44《围栏详情关联内容》。先读00总指挥和本任务对应原图，只处理“FencePage”。不重画三维厂区、不改围栏后台规则。完成后用雷电展示浅深效果、原入口闭环与回退点；没有明确连续执行要求时，不启动下一任务。

