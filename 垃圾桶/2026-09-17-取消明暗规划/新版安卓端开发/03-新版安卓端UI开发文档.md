# 新版安卓端 UI 后续开发依据

> **V3失效声明（2026-09-17）：以下为历史稿，不再作为开发指令。** 用户已否定并回退整体换肤、三栏导航和页面职责重排。当前只允许在回退版上按浅／深预览图1:1复刻及小幅增补。旧稿的视觉取舍、模块迁移、素材禁用和默认三主入口等建议全部停止执行；事实性代码／素材线索可供核查。当前以 [10-开发指南](10-安卓开发指南-预览图复刻与微调.md)、[11-逐页说明](11-安卓逐页开发说明与跳转保留表.md)、[12-强限制](12-安卓开发强限制与验收清单.md) 为准。下方旧修订说明仅保留历史含义。

修订：2026-09-17 · V2 评审稿。**本轮不实施。**本文替代旧“在WearApp上直接叠浅色插画、路由不动”的说明。页面分配以 [07](07-安卓页面职责与冲突整改方案.md) 为准，组件和主题以 [08](08-安卓统一视觉与明暗主题规范.md) 为准。

## 1. 实施前必须确定的边界

| 边界 | 本稿要求 |
| --- | --- |
| 产品 | 客户§10.3：App承担通话调度与收告警。三入口为待审核安卓适配建议 |
| 架构 | 客户§10.1不复用旧脚手架；先确定新工程边界、接口与数据迁移，不默认批准复用WearApp壳层。旧源码用于现状、字段与行为参考 |
| 范围 | 按人组织设备与位置，统一告警；不新增AI、考勤、完整两票、巡检工单或大型数字孪生 |
| 依赖 | 每个按钮注明真实API、权限、设备能力、回执及不可用原因；没有接口不增加虚假成功交互 |
| 历史兼容 | 旧通知ID、处置历史、草稿、角色与厂站约束有迁移方案；是否保留原URL另做映射，不要求旧路由全部照搬 |
| 辅助能力 | 交接、轨迹、复杂处置按角色审核；保留数据不等于每个旧按钮必须留在手机主页面 |

## 2. 功能与页面的施工顺序建议

| 阶段 | 交付内容 | 可审核结果 |
| --- | --- | --- |
| 1 架构与接口盘点 | 新工程边界、账号／人员／设备ID映射、厂站权限、能力和状态契约、旧深链迁移表 | 无真实接口的动作有明确依赖，不凭姓名猜关系 |
| 2 统一设计基础 | 深浅语义色、字体、间距、列表、表单、按钮、弹窗、空错状态 | 同数据双主题稿；默认客户暖石深色，普通与大字号均可操作 |
| 3 授权与页面骨架 | 登录、授权选站、三主入口、返回栈、主题持久化、全局通话／SOS提示 | 未认证不显站；选站全局一致；主题与登录、路由和业务状态解耦 |
| 4 通讯主流程 | 找人、装备终端、准备、实际通话、TTS、记录 | 单呼／组呼／发起端如实显示；失败不静默降级，来源入口可返回 |
| 5 统一告警 | 列表、独立详情、发生时位置、同人联系、授权确认／说明、历史 | 不另造SOS或核验状态机；草稿和冲突恢复有效 |
| 6 关联与个人设置 | 人员／装备／位置／作业只读上下文、我的装备、接警帮助、获准兼容子页 | 不重建现场管理中心；本人装备与全站台账分离 |
| 7 联调验收 | Docker API、雷电导航与主题、真机接警、RTC、穿戴协议、分层位置 | 分项记录实际结果、缺口与负责人；UI通过不能替代设备闭环 |

三入口和布局审核后才开展实现；此表是顺序建议，不是承诺当前后端具备全部能力。

## 3. 必须保持的业务语义

- 登录用户账号不等于现场人员；以稳定ID和明确关联为准。
- 同人一处当前主位置；历史告警位置与当前位置分开，时间、区域、层、来源和质量可读。
- 来源设备不等于联系设备。安全带可提供告警，但联系应指向同人可用通信终端。
- 通话创建、振铃、媒体接通、挂断、告警确认与结案分别表示不同事实。
- 组呼和帽端发起是客户目标，尚无完整链路时明确待依赖，不以取首个设备单呼冒充完成。
- TTS下发受理不等于对方听到；无播放回执不能提示已收听。
- 所有告警沿用服务端合法状态与版本；移动端精简入口不丢责任历史或绕过权限。
- “未处理”“未关闭”“未读”按服务端各自定义，不能直接共用计数。
- 正式数据空缺、失败、未知和演示分别呈现，不补固定姓名、设备、票号或在线状态。

## 4. 主题切换的实现约束

同一套布局和业务状态使用两套语义主题。登录和顶栏一键切换，“我的→外观”读写同一偏好；首次深色，之后记住用户选择。主题不得重置账号、当前站、返回栈、筛选、滚动、已选人、草稿、地图视点或RTC会话，不重复提交、重拨或发TTS。

两主题覆盖所有主子页面、弹层、系统栏与第三方容器。视频和照片保持原色；系统权限对话框外观由系统决定。客户深色色值、建议浅色色值及待验收对比目标完整见08，不在本文件维护第二套色表。

## 5. 后续验收清单（尚未执行）

- [ ] 登录、首次选站、返回与通知深链、无权限与过期会话正确。
- [ ] 人员及终端选择准确，单呼、视频、TTS结果真实；组呼与帽端有独立联调证据。
- [ ] 带来源SOS解析同人终端，来源与历史位置未被当前记录覆盖。
- [ ] 统一告警筛选、详情、授权动作、409草稿恢复与返回列表正确。
- [ ] 切站清理旧站对象，草稿按账号／站隔离；通话中禁止切站。
- [ ] 双主题逐页一致，大字、TalkBack、触控和实际色彩对比通过。
- [ ] 在登录输入、处置草稿、选人、通话、地图和SOS提示中切换主题，状态不丢失。
- [ ] 雷电完成界面与导航验证；后台接警、真实RTC和穿戴数据另有真机／硬件证据。
- [ ] 客户要求与已完成能力逐条核对，未完成项仍在依赖清单。

本轮不运行新UI验收，也不宣称上述项目已通过。连接环境参考 [06-雷电网络故障原因与修复记录.md](06-雷电网络故障原因与修复记录.md)。
