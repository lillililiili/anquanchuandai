# 预览图差异与局部例外登记

本文件防止“按图复刻”和“保留现有基础”产生隐含的大改授权。下表是任务计划中的显式差异，不是已经实施或已经获视觉验收的结果。除表内所列，不默认允许任何页面结构变化。

## 1. 需求优先级

用户最新要求明确：保留原功能任务，仅取消亮暗切换；当前按浅色预览图微调，我的以用户最新图为准；本轮只修文档。客户Word继续约束业务真实性与接口含义。Word中的无插画登录、暖石色、App定位及不复用旧脚手架，与当前按图／保留基础要求并非完全一致；不得再自行选择整体重构来“解决”这些差异。

本轮按父目录10的明示处理：视觉分别依预览图，业务遵守统一人员／设备／事件与真实能力；不宣称与Word所有视觉要求完全一致。产品名和主按钮只允许已列明的原位文字校正，不能因此去掉整个登录头图。

## 2. 有明确原图的少量子视图

| 编号 | 对应任务 | 已知差异 | 局部处理边界 | 明确禁止 |
| --- | --- | --- | --- | --- |
| E1 | T20 作业详情03 | 原图无四栏，当前TaskPage在现场分支 | 仅把 `/tasks/:id` 对应详情展示到根导航层，原URL、taskId、来源返回继续兼容；允许app.dart局部parentNavigator承接及必要根key，列表和其他查询不变 | 全部查询页一起移出壳层，改默认页或四栏数量 |
| E2 | T25 核验02 | 当前处理表单在事件页工作区，原图为无底栏核验视图 | 原处理入口打开全高局部视图，外部传同一EventController；上层显示无底栏，下面消息页保留 | 新造事件状态机、全部事件自动改核验、迁走原操作 |
| E3 | T28 SOS04 | 当前SOS在事件详情内，原图为无底栏SOS视图 | 只针对SOS内容打开局部视图，共用同一事件与实际通知上下文 | 独立SOS事件库、改变授权厂站校验 |
| E4 | T31 视频05 | 当前会话在通讯页，原图为无底栏大画面 | 局部全高视图渲染同一会话；退出展示返回通讯；真实挂断仍原控制 | 新开RTC会话、改通讯为多步骤新首页 |

E2—E4可使用原页面持有controller的根Navigator局部页面包装，不修改所有分支。包装不得在build中反复push，不因界面刷新重开，不dispose父页拥有的controller。若既有导航条件不能用这一局部办法达成，先列具体差异和更小替代方案，不能绕过总限制重写壳层。

## 3. 装备子视图00

T17只增加本人装备的轻量显示，原EquipmentPanel数据复用；T18增加可选产品卡显示，默认旧调用不变；T19连接原首页两个装备入口和我的入口。三个任务分开验收。

图00包含四栏；从我的进入选中我的，从现场进入则保留现场来源。保留来源是原入口语义的小差异，不为了截图固定选中我的而强制切主分支。全站设备列表仍是全站列表，不能改名冒充本人装备。

## 4. 已有但图中未出现的模块

现场待认领、指标、工具和作业列表继续保留原位置，不自动折叠或迁移。

我的已按用户新图完成局部调整：本人装备由菜单打开详情，接警由通讯服务打开，交接位于设置。T17、T47、T48沿用这些入口，不再遵守旧“全部留首页原位”规则，也不把例外推广至其他页。

## 5. 我的已实施例外与剩余差异

| 编号 | 状态 | 当前边界 |
| --- | --- | --- |
| E5 我的主页 | 已开发部署，待用户审核 | 新图人物头部、五行资料、服务卡、菜单和账号按钮；T13—T16不重复开发 |
| E6 我的底栏比例 | 部分覆盖T02 | 我的60dp，其余原值；保留四栏与原图标，不全局改高 |
| E7 本人装备子页 | 部分覆盖T17 | 已有菜单与局部详情，当前无底栏；与图00带底栏存在差异，补页头时登记而非默默重构导航 |

原T01及双主题生成／切换要求取消，原图仍保存作资料。当前图面差异必须记录真实状态，不能用已取消的深色交付阻塞其他功能任务。

## 6. 业务内容与原图示例

图上人物姓名、票号、在线状态、SIP ID、示例照片和通话时长用于视觉比较，不能硬编码为生产事实。可以在明确UI演示下使用同构样本做截图，但已有API不得改成mock来凑图。

核验附件用本地交互先完成；没有远程上传不删除卡，也不报已上传。组呼、帽呼、SOS协助同理：入口和准备UI保留，执行状态真实。安全带断连不得自行判违规，位置未知不得凭图片估算楼层。

## 7. 超范围变更登记格式

后续遇到表外差异，只新增一条清楚记录：任务ID、原图页与区域、当前实现、拟改前后画面、保留的入口、受影响文件与回退方案。状态写“提出，未实施”。没有用户对该具体变化的明确指示，不把它夹带进正在执行的任务；不影响其他已明确的小项继续推进。
