# 主题、局部视图与素材衔接说明

> 执行进度（2026-09-17）：用户已授权并完成T01开发及雷电覆盖安装，开发自验通过，待用户查看；T02—T49未开始。下文“仅文档／未实施”指原编制轮，当前实施记录见[T01验收说明](验收记录/T01/说明.md)。
本文件定义任务之间的最小约定，防止每个任务自建主题、复制业务控制器或生成不一致背景。以下新增名称是**后续实施契约**，不是当前已有代码。本轮不创建任何Dart、Kotlin或图片文件。

## C1：一个安装级外观状态

由T01提供，后续任务消费。候选文件 `lib/wear/preview_appearance.dart`，复用工程已有shared_preferences，不升级依赖。

```dart
enum PreviewMode { light, dark }
// 后续实现约定，不是要求把此声明原样粘贴进工程。
// PreviewAppearanceController extends ChangeNotifier
//   PreviewMode get mode
//   Future<void> load()
//   Future<bool> setMode(PreviewMode value)
// PreviewAppearanceScope.of(BuildContext context)
//   -> PreviewAppearanceController
```

偏好键约定 `wear.preview.appearance`，保存 `light` 或 `dark`。首次及读取失败默认浅色，保持回退版本初次观感；本稿不把它说成客户Word要求。保存失败当前界面仍可切换，原位提示偏好未保存。并发写入按顺序，旧读取不能覆盖用户刚选择的值。

控制器只拥有外观，不拥有账号、厂站、路由、草稿或RTC。创建一次、释放一次；MaterialApp和业务控制器不要随mode换Key重建。

T01只让设置面板可见切换，T02只适配导航，每个页面局部依次接入。可以用局部Theme／外观值实现阶段隔离，但所有区域读同一mode；禁止为每个页面另存偏好。尚未适配区域在阶段包列出。T49在覆盖后接入根主题，解决路由外弹窗与通用表面；不以此更改已校准几何。

所有主题色来自对应原图的内容区，分别核对背景、卡底、边界、主文、辅助文、主按钮、状态色。不是把上一版暖石色表重新复制回来。生产构图不能由ThemeData默认样式决定。

## C2：本人装备轻量视图

T17提供 `lib/wear/queries/my_equipment_page.dart` 中的 `MyEquipmentPage({super.key})`，只展示当前账号关联人员的装备，复用已有 `EquipmentPanel()`；不另建装备仓库。

当前 `EquipmentPanel({super.key, this.personId})` 已存在。T18可新增可选展示参数：

```dart
enum EquipmentPresentation { existing, productCards }
// EquipmentPanel 的新增可选参数：
// EquipmentPresentation presentation = EquipmentPresentation.existing
```

原调用默认existing，新视图显式productCards。`personId == null`仍表示当前账号装备，不能改成全站；传personId仍表示对应人员。T19连接首页两个本人入口与绑定历史，保留来源分支与返回。

## C3：人员、终端与发起端

继续现有PersonOption、CommunicationDevice等模型，不为换肤重建通讯域。

- 已选人员集合按稳定personId去重；独立设备用deviceId单独记录，显示数量时不要混为人数。
- 一个已选人员可以有多台装备，执行时在原卡或局部面板确认有能力且被授权的目标终端。
- 发起端是手机或本人安全帽，与联系对象不同。不要因本人绑帽就仍调用本机RTC并称为帽端呼叫。
- T10解决原发起区的确认与能力反馈；T22、T30、T40引用这套目标语义，不复制呼叫流程。
- 缺组呼／帽端执行能力仍可完成UI准备、选择与确认，远程结果显示未开通；不能偷偷只执行第一个目标。

## C4：保留旧链接的操作意图

T11在现有 `/communications` 上增补可选 `intent`，取值 `tts`、`voice`、`video`。保留deviceId、personId、eventId及原video=true/1。

意图只定位原通讯页相应区域。参数未传时保持原行为；不能自动提交TTS或自动拨号。未知意图回到原通讯内容并保留对象，不报整页错误。T40复用此约定，不新建一套mode参数。

## C5：核验、SOS与作业局部承接

现有事件控制器文件为 `lib/wear/events/event_controller.dart`。T25、T28仅包装它，不新增第二个事件状态机。

```dart
// T25 VerificationView({required EventController controller})
// T28 SosView({required EventController controller})
```

controller由原事件页拥有，局部视图订阅／取消自己的监听，但不得dispose外部controller。eventId、权限、草稿和写入状态来自该同一实例。列表到原详情不自动变成核验；只有原处理入口进入核验。若局部视图返回时有草稿，沿用既有保存规则。

作业仍由原taskId与接口驱动，局部无底栏只针对图03，原 `/tasks/:id` 继续兼容。具体四项例外见04，不将全部查询页移出壳层。

## C6：本地照片UI不依赖上传接口

已核查当前pubspec及代码，未发现image_picker／file_picker或既有选图封装。因此T27不能假定已装选图库，也不能只写“复用已有能力”然后停下。

在不新增依赖的前提下，T27采用**现有Android MethodChannel的局部扩展**，只为图02选照片：

- Flutter侧由 `verification_attachments.dart` 调用 `rolling/wear` 的新增方法 `pickVerificationImages`，参数 `remainingSlots` 为0—4。
- Android侧限于 `android/app/src/main/kotlin/com/rolling/intelligence_headband/MainActivity.kt`，保留pushAppKey／pushVendors等原方法。使用系统文档图片选择器，允许多选；取消返回空列表，不报错误。
- 回传列表元素只含本地应用私有副本路径 `path` 与 `mimeType`；按剩余数量校验，读取失败原位提示，不申请整库读取权限或擅加新存储权限。
- UI最多4张、能预览删除；使用系统已有选择器，不新做相册管理页面。图中“支持照片”不自动扩展为拍照摄像或视频上传。
- 本地草稿的归属键含账号／厂站／eventId；文件失效原缩略图位置提示重新选择，不当成已上传。正文真实提交前明确哪些照片未上传，用户不能被静默丢附件。
- 无后端上传时，本地选择／预览／删除／草稿仍可验收；真实上传与远程附件回执不伪造。

这项原生改动只服务于一个看得见的附件区域，与T27同时交付，不拆成后台基础建设任务，不改Manifest权限或Gradle依赖。如果系统选图差异使任务增大，则先拆T27a本地附件卡和T27b系统选图闭环，两个都必须有可见UI结果。

## C7：视频展示复用同一会话

T31候选 `lib/wear/communications/call_view.dart`：`CallView({required CommunicationsController controller})`。当前控制器定义在 `lib/wear/communications/controller.dart`。

容器不拥有RTC实例，不创建新通话，只监听现有阶段与渲染现有视频。T32的静音／音频路由／挂断调用原controller／RTC适配器，不能只切换图标。退出显示视图与结束实际会话分开；返回原通讯区继续显示同一会话。

## S：按页面登记素材与实际引用

下面都是拟新增路径，相对安卓工程，不是本轮已生成文件。每项背景任务包含生成、质量核对、Flutter引用、切换与雷电截图，缺少任何一项不能只把图片交付标为任务完成。

| 素材号／任务 | 参考 | 必需深色候选路径 | 浅色策略与用途 |
| --- | --- | --- | --- |
| S03／T03 | 07页头 | `assets/field-brand/preview-v3/home-header-dark.png` | 复用天空／头像；仅校准本页页头 |
| S04／T04 | 07作业卡 | `assets/field-brand/preview-v3/work-scene-dark.png` | 与现有作业场景对应，保留通道人物关系 |
| S08／T08 | 06页头 | `assets/field-brand/preview-v3/comms-header-dark.png` | 浅色电话人物保留，深图不加人物 |
| S13／T13 | 01页头 | `assets/field-brand/preview-v3/mine-header-dark.png` | 浅点赞人物，深简洁无人物 |
| S20／T20 | 03页头 | `assets/field-brand/preview-v3/task-header-dark.png` | 浅作业人物，深图相应低细节底 |
| S25／T25 | 02页头 | `assets/field-brand/preview-v3/verification-header-dark.png` | 浅核验人物及警告，深无人物 |
| S28／T28 | 04页头 | `assets/field-brand/preview-v3/sos-header-dark.png` | 浅对讲人物，深按原图留白 |
| S29／T29 | 04示意地图 | `assets/field-brand/preview-v3/plant-map-dark.png` | 原地图配对，仅示意用途，道路厂房方位保持 |
| S33／T33 | 08登录 | `assets/field-brand/preview-v3/login-header-dark.png` | 浅人物与装备静物，深厂房背景 |

以上为按页资源定位，不要求无纹理的深色区也凭空画建筑。原图深色只有低细节色底时，新背景同样保持该低细节色底，禁止为了“生成一张图”加入主体。路径可在任务范围内根据最终裁切命名微调，但登记与代码引用必须同步。

若确需重做浅色配套，在同一任务新增对应 `*-light.png`，保留旧文件，说明为何现有图不能满足对应原图。不要为统一而批量重画全套。产品照片、验证码、附件、真实视频通常保持原内容，通过局部容器适配。

## 图片生成约束模板

> 只生成当前任务参考图【页编号／主题】中【具体背景区域】的背景素材。保持原图主体位置、空间留白、工业风格、视角和裁切关系。浅色有人物、深色无人物时遵守各自原图，不强行同构。不要生成UI文字、品牌字样、按钮、手机边框、状态栏或任何新模块；不添加参考图中没有的主体。地图素材仅作明确示意，不能作为实测位置。先核对原图再生成，并在本任务安卓UI中完成引用和主题切换。

实际执行生成时必须先查看相应原图和拟编辑本地图片，遵守imagegen技能；本轮不调用生成工具。每张记录来源图、提示词、相对路径、尺寸、SHA256、页面位置和浅深映射，不能用已撤销素材当输入基线。
